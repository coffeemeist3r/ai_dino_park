# Cycle 135 — Structure Handoff

**Intent:** Close Milestone 14's last open arc. 481 handed a ground's **work** call to its council and
deliberately left the **spend** call with the provider — "the unchanged call sits beside the changed
one as a live control." The control has now run four cycles (481 → 484's term → 485's bill → 486) and
reported: the voted call is the one with a term, a turnover beat, and a feedback loop from the
skyline, and the unvoted one is still re-set silently by whoever out-banks everyone else. 487 retires
the control and makes the ground's two calls the same kind of thing. It reuses 481's tally wholesale —
the majority arithmetic is identical, only the enum differs — so the diff is a generic and a mirror of
`decideWork`, not a second governance system.

**Added to Structure Track:** BACKLOG-492 — a vote nothing can change (queue was 3 open, below cap X=4).
It is the question 487 forces: with both calls now votes, every seat still votes a hard threshold on
one *name-seeded* axis, so a council is unanimous by construction and no event in the park can turn
one. That is why 485's bill had to **override** the vote rather than persuade it, and it is the next
real thing on this spine.

**Chosen this cycle:** BACKLOG-487 — the spend priority (463) runs through the same vote the work call
uses. Milestone 14 structure arc; last unchecked box in the milestone.

**Collision check:** the lore pick (416) is `world/tic.ts` + the solitary-stretch block of `WorldScene`;
this is `world/governance.ts` + the governance block of `WorldScene`. Different modules, different
regions of the scene, one shared file. Clean two-track fire.

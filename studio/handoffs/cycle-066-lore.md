# Cycle 66 — Lore Handoff

**Theme:** Even nothing reads as character. Cycle 65 made the bowl *legible* — a per-dino glyph now tells you what each one is doing (295). But the most common state by far is the goalless 🚶 wander, and five wandering dinos still read as five identical drifting squares. This cycle takes the legibility win one step into distinctness: a dino with no goal should *fidget in its own way* — a bold one paces, a timid one glances around, a curious one pokes at the glass. Idle time becomes a personality tell instead of dead time.

**Added to BACKLOG:**
- BACKLOG-301 [emergent] Contagious fidget — an idle quirk can briefly ripple to a near neighbour (mirroring), a one-shot borrow.
- BACKLOG-302 [emergent] Mood-shaded fidget — the same dino's quirk darkens with its mood (a sulker mopes where a content one paces).
- BACKLOG-303 [pokemon] Signature quirk in the dossier — the book names each dino's idle fidget as a one-line fingerprint.

**Suggested next-up:** BACKLOG-298 (Idle fidgets) — the already-queued spine seeded last cycle off 295. It turns the readout's idle 🚶 state into a small temperament-shaped idle quirk, so the bowl's resting state stops being five interchangeable wanderers. Pure trait→quirk mapping over the personality axes the sim already seeds (010), surfaced as a transient mark/motion on the existing per-dino mark convention; no LLM, no save change. The headline distinctness win this cycle and the spine 301/302/303 extend. Designer free to override.

**Idea Box:** the operator's `[new]` **stash-ahead art policy** nudge — **still deferred** (held under Open). BACKLOG-296 (resource/cairn pixel props) remains the Artist's renderable-now subject, so no art fire need no-op for want of unblocked work; the broader "author rigs ahead of their host system" rule only earns a decision if a future art fire would otherwise idle with nothing but terrain-blocked work (033) left. No change this cycle.

**Note to Structure-smith:** the cap-rule queue sits at exactly X=4 open (145 / 274 / 293 / 294) — drain, don't invent. One caution worth flagging: **BACKLOG-293 (crafted-object persistence) may already be shipped** — the cycle-64 close note for 286 says cairns "ride an additive `cairns` save field re-rendered on load," which is 293's whole acceptance. If so it's a clean ABANDON-as-duplicate, not a build; recommend the structure pick land on real unshipped work (274 populate-grove is the spine that unblocks 294, or 145 the plantable plot). Either way, keep clear of this track's WorldScene `forceStep` idle-motion lines (298 is per-dino idle quirk; pick a structure item whose files don't collide there).

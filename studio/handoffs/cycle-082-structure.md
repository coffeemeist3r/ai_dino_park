# Cycle 82 — Structure Handoff

**Intent:** Make the divergence the build arc has been building toward *legible*. Zones now gather (314),
bank per-zone (328), lean their own mix (348), grow (349), and trade directionally (356) — but the plaque's
Stores line only shows the keeper's *active* zone. The player can't watch the bowl and grove economies pull
apart without walking between them. 357 surfaces **both** zones' piles at once, the way the zone tally (316)
already shows both populations — closing the read-out gap so the divergence the last ten cycles built is
finally visible standing still.

**Added to Structure Track:** none — drained from queue (4 open: 357/358/377/378 ≥ X=4).

**Chosen this cycle:** BACKLOG-357 — both-zone stores readout. Top unblocked item; all deps (328/316/348)
shipped. File-disjoint from the lore pick (374 = `world/loner.ts` + `eatFood`; 357 = `ui/plaque.ts` + the
plaque-stats builder), so the two-track Coder fire stays clean.

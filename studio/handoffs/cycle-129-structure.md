# Cycle 129 — Structure Handoff

**Intent:** 479 derived a set of deciders and gave them nothing to decide — a council was a badge on the
lens and a line in the book. That was the right order (a vote needs an electorate first), but it leaves
the park with governance that is still a monarchy of one: both of a ground's calls read a single dino's
temperament. This cycle hands the **work priority** (473) to the council by majority, provider breaking
ties — which is **BACKLOG-031**, queued since cycle 1 and deferred every cycle since for exactly one
reason, that there was no set of deciders to hold the vote. There is one now. The spend priority stays
the provider's on purpose: one call moves, so the compatibility seam is testable against the other.

Its "Blocked on 479" tag was stale — 479 shipped cycle 127 (the Lore-smith flagged it). No file
collision with the lore pick: 389 lives in `pecking.ts` and the rush branch of `stepDinos`, 481 in
`governance.ts` and `workPriorityFor`.

**Added to Structure Track:** BACKLOG-484 (the seat has a term — the live-derived electorate will flicker
once it decides something), BACKLOG-485 (the bill reaches the call — 480's upkeep is inaudible to
governance). Queue was at 3 open, below cap X=4.

**Chosen this cycle:** **BACKLOG-481** — the work priority set by council majority off the seated
members' temperaments, the provider breaking ties, with a one-off ticker beat when the ground's call
actually changes. A ground that seats nobody, and a ground that seats exactly its provider, must both
read bit-identically to today.

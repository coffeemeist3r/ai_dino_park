# Cycle 131 — Structure Handoff

**Intent:** Close Milestone 13's structure half by paying the debt the milestone itself created. Three cycles
ago this park had two derived per-zone standings; 479 made it three, and the third arrived exactly as the
first two had — its own tally, in its own module, recomputed on its own hook, read by hand in the collection
book. `ZONE_TERRAIN` (449) taught this lesson once already for terrain and stated the promise in its own
header: *a fourth zone is a row, not three branches*. **482 applies it to roles.** One pure per-zone standings
module with a single shape (`{ zone, kind, dinoId(s), since }`), one derivation the book, the lens and the save
all read, so the fourth standing — and 484 will want one, and 487 after it — is a row and not a fourth code
path. Now, and not later, because two of the three queued structure items stand *on top of* the standings
(484 gives a seat a term; 487 hands it a second call), and both are cheaper written once against a folded
module than twice against a sprawl.

Collision check against the lore track: 404 reads the memory ring for **recency** and writes into the
greeting/dialogue path; 482 reads the zone **tallies** and writes into the standings/book/lens path. They meet
in exactly one file — the collection book — and in different sections of it (404 adds no book line at all;
402's manner line, which 404 sits beside conceptually, is not being folded). Sequence 482's book edit after
404's dialogue work and the two never touch the same function.

**Added to Structure Track:** **BACKLOG-486** (the run, not the spec — the general e2e parallel-load fix the
cycle-130 validator declined to bury inside 430's note) and **BACKLOG-487** (the other call goes to the
council — the spend priority 481 left behind). Queue was at 3 open, below cap X=4; now 5.

**Chosen this cycle:** **BACKLOG-482** — one place the standings are derived. Pure module, additive save, no
behavior change intended: the same dino holds the same standing before and after, and the suite standing
perfectly still is the evidence. **This closes Milestone 13's structure half.**

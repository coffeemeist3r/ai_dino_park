# Cycle 112 — Lore handoff

## Milestone status

**Milestone 7 "The economy has weight"** is ACTIVE with a single arc left: the structure arc
**BACKLOG-460 (The draining zone)**, which the Structure-smith picks this cycle to close the
milestone. All three M7 lore arcs shipped (457/458/459).

## Lore pick — BACKLOG-464 [emergent] Last one standing (NEW)

**Off-milestone justification (one line):** M7's lore checklist is closed, but 460 mechanizes an
exodus and this is its *human* read — the dino the floor keeps behind. It rides the exact theme
M7 closes on (a zone hollowed by scarcity migration), so it pairs the milestone-closing structure
arc with a matching feel beat rather than opening an unrelated thread a cycle before M7 ships.

**The beat.** 460 gives a draining zone momentum and a floor (it thins but never vanishes — at
least one resident always stays). That last resident is a *character*, not a counter. When a zone
has hollowed to its final dino, that dino feels the quiet: a wistful 🍂 "gone quiet around here…"
beat and a `you're the last one left in <zone>` memory that rides recall into its next greeting.
Observable, distinct, and deathless — the emotional cost of the mouths-move-toward-plenty economy
the milestone spent seven cycles building. The ground empties; the one left behind *notices*.

**Composition.** Reuses 460's declining read (`isDeclining` + the per-zone peak high-water mark):
the beat fires exactly for a zone that `isDeclining` **and** is down to `heads === 1` (the floor
case). Deduped against the dino's own memory ring so it reads as a moment, not a tic — a zone that
repopulates and drains again can sound it afresh. No new save field (memory already persisted).
NPCBrain boundary intact (deterministic strings; the memory colours the LLM greeting where a
device allows, exactly as 457/459's traces do).

**Deferrals seeded, not stubbed:** a *gossip* channel for the emptiness ("the Fernreach's gone
quiet") is a natural follow-up but stays out of this cycle — the beat lands as a private memory +
bubble first, the same foundation-first shape every gossip arc was seeded from.

## Idea Box

No open entries. Nothing to seed or decline this cycle.

## Cycle bookkeeping

Both cycle-111 tracks resolved APPROVED, so the cycle number bumps 111 → **112**.
`state.currentItem = BACKLOG-464`.

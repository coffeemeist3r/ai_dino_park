# Cycle 68 — Lore Handoff

**Theme:** The body language leaves the page. Cycle 66 made idleness read as character (298 — each dino fidgets its own way), cycle 67 *named* that fidget in the collection book (303). The fidget is now a kept fingerprint — but it still only ever happens while a dino wanders, or sits as text in a menu. This cycle the bowl should *perform* it at the keeper: the welcome-back beat (112), today a generic 👋 from your closest dino, should greet you in that dino's own signature body language ("Rex paces eagerly at the glass" vs "Twitch peeks out from behind, then bolts over"). Quiet, distinctness-forward, no new system — taking a tell the sim already produces and making it the shape of a moment the player already gets.

**Added to BACKLOG:**
- BACKLOG-310 [emergent] Quirk shaded by feeling — a transient mood colours the signature fidget so motion reads state, not just temperament.
- BACKLOG-311 [social] Greeting names the quirk — the keeper's greeting reply leans on the dino's live fidget (deterministic fallback, LLM colour behind NPCBrain).
- BACKLOG-312 [pokemon] Quirk in the scan — Lux's Field Scan reports the dino's resting quirk alongside its axes.
- BACKLOG-313 [emergent] Caught habit — a dino slowly picks up a near-constant neighbour's tic (capped, the 043 drift shape).

**Suggested next-up:** BACKLOG-306 (In-character homecoming) — the already-queued payoff of 298/303. It reads the existing `fidget()` quirk (deterministic trait→quirk, no model, no save) and folds the label into the homecoming welcome-back bubble + beat, so the closest dino greets you as unmistakably itself. Pure surfacing over `homecoming.ts` (a settled, pure module) + the WorldScene bubble text — **zero collision** with the heavy movement/zone work the Structure-smith is taking this cycle (it never touches `forceStep` or per-dino wandering). High distinctness payoff, near-zero risk — the right light lore half to pair with a heavy structure half. Designer free to override.

**Idea Box:** the operator's `[new]` **stash-ahead art policy** nudge — **still deferred** (held under Open), unchanged. The Artist has renderable-now work queued (the plot's 🌱🌿🍓 crop stages for 145, then grove ambient props once 308 lands), so the broader "author rigs ahead of their host system" rule still doesn't need a verdict this cycle.

**Note to Structure-smith:** the structure queue sits at **4 open** (274 / 293 / 308 / 309) = cap X=4, so **drain, don't invent** — pick the top unblocked item, no brainstorm. BACKLOG-294 (grove terrain) shipped last cycle, so the grove now reads as its own place but stands empty — **BACKLOG-274 (populate the grove)** is the natural, highest-value pick now: assign home zones, migrate dinos, and gate proximity interaction so the grove becomes an inhabited place, not a pretty empty room. Its render-filter half already shipped (operator 2026-06-20); the remaining home-zone/migration/interaction work is heavy and lives in `forceStep`/zone glue — which is exactly why my lore pick (306) deliberately stays out of movement. BACKLOG-293 (crafted-object persistence) remains a confirmed ABANDON-as-duplicate (286 already persists the cairn) — flag for the Validator to formally close, it shouldn't keep riding the queue.

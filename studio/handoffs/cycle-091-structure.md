# Cycle 91 — Structure Handoff

**Intent:** The world at a glance. Cycle 90 made each edge name its neighbour; this cycle
the whole chain becomes one picture — a zone-map lens on the V-lens ring, drawn straight
from the 383 adjacency table with the 316 occupancy counts and a keeper dot. Milestone 1
structure arc 2. The other open milestone arc (426, save envelope) is deliberately skipped
this cycle: the lore pick (103) persists a persona cache into the save, and two tracks in
the save path is exactly the collision the pick rule exists to avoid — 426 is next cycle's
natural pick, and 103's cache becomes the first thing its migration rail carries.

**Added to Structure Track:** none — drained from queue (4 open = X).

**Chosen this cycle:** BACKLOG-425 — zone map lens (adjacency table → labelled boxes +
occupant counts + keeper dot, one more page on the existing V-lens ring). File-disjoint
from lore 103 (zones/lens UI vs ai/persona + save); shared WorldScene.ts only in different
methods, the standing precedent.

**Skipped:** 417 / 418 (off-milestone — the milestone arcs drain first), 426 (save-path
collision with 103, above).

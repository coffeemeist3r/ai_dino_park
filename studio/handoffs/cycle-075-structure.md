# Cycle 75 — Structure Handoff

**Intent:** Make the split world *legible*. The structure track has spent seven cycles building two zones the player can barely tell apart from inside one of them — you only know where the cast is by walking there (143→274→308→314→334). Before splitting the economy further (328/329), surface the split itself: a small plaque readout of the keeper's active zone and the population in each. A cheap, load-bearing legibility beat that every later two-zone feature benefits from.

**Added to Structure Track:** queue had 3 open (`< X=4`), so brainstormed 2 foundation-first items, both deepening the *existing* two zones (no third zone — the operator's standing call is to drain two-zone depth first):
- BACKLOG-348 [emergent] Zone resource bias — each zone leans its own resource mix (grove → branch, bowl → stone) so the two economies gather different things; gives 329 (carry between zones) a point.
- BACKLOG-349 [core] Grove plot — a second plantable plot in the grove so the second zone farms its own crop (the plot is bowl-only today, 308).

Queue now 5 open (316/328/329/348/349) — healthily above X.

**Chosen this cycle:** BACKLOG-316 — Zone indicator. Top unblocked, smallest, and it makes every other two-zone feature readable. Files (plaque.ts / zones.ts / WorldScene plaque block) are disjoint from the lore track's gossip-cascade + crossDino edits — clean two-track fire.

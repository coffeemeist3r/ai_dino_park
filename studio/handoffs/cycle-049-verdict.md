# Cycle 49 — Verdict

**Verdict:** APPROVED

**Item:** BACKLOG-185 — Word of the cold

**Rationale:** 8/8 acceptance criteria on a clean build; 438 unit / 164 e2e green, the only red the catalogued cold-boot parallel-load flake (5 early specs — controls-help ×3, cycle-002, cycle-003 — every one green re-run isolated single-worker, where the two new cycle-049 specs and both cycle-020 gossip specs also passed 14/14). This is the model single-cycle item: cycle 43 queued it, cycle 48's verdict named it the natural next cold-arc pick, and it lands as a pure module plus one branch in one existing seam. The whole gossip spine is reused verbatim — `coldWordLine` carries the existing `RUMOR_MARK`, so the 1-hop / non-re-shareable / `isShareable`-false semantics come for free, no new propagation code — and the cold-news detector keys off `COLD_NEWS_TOKEN`, a substring of `coldMemory()` that a unit test pins so the two can never drift. The change is genuinely additive behaviour, not a reskin: in `converse` the speaker has just filed `you ran into <b>` as its newest memory, so the generic `pickGossip` (most-recent-wins) would never surface the cold — the cold-first branch is exactly what makes a cold-slept dino *lead* with its hardship instead of the latest trivia. Generic gossip is byte-unchanged and pinned green by the untouched cycle-020 spec; the 🥶 log line is a clean sibling of the 🗣️ one. No save-format change (the memory store has carried this since cycle 11 — the ninth-plus cycle running with zero save growth), no new deps, NPCBrain never in play, boundary grep clean. The three dev hooks (`__spreadColdWord` / `__coldWord` / `__rememberCold`) mirror the existing `__spreadGossip` exactly and let the whole feature be driven headless without a model or a faked winter night.

**Notes for follow-ups:** The gossip channel now has its first real subject, and the cycle-49 lore queued the rest of the mill behind it. 217 (secondhand sympathy → a visit) can read a listener's freshly-planted cold word to arm a comfort drift. 218 (grows in the telling) is the one item that will need to relax the strict 1-hop `RUMOR_MARK` rule — it should layer a capped per-hop drift on top, not fight it. 219 (rumor mill in the book) already has everything it needs: the listener's memory carries `<speaker> told me: …`, parseable straight into a "heard from X" book line. 220 (same news, different heart) hangs off the listener's temperament at plant time. 221 (word reaches the keeper) wants a spread-count across the cast, which the per-listener plant makes countable. 222 (old news goes quiet) is the freshness gate this cycle deliberately left out (a cold-slept dino currently tells everyone it meets until the memory ages out of the 6-slot ring). The cold arc is now legible in three registers — private memory (179/184/208), audible cry (194), and travelling word (185).

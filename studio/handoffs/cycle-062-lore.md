# Cycle 62 — Lore Handoff

**Theme:** The name shrinks to a nickname. Cycle 61 (276) earned the keeper its *designation* in a
fond dino's mouth — "There you are, AETHER-1!". The next rung of the greeting ladder is intimacy:
the dino closest to you of all drops the formal designation and uses the *nickname* the unit carries
("There you are, Aki!"). Same warm line, a smaller, fonder name — the difference between a friend who
knows your callsign and one who knows what your friends call you.

**Added to BACKLOG:**
- BACKLOG-283 [social] Nickname only once it's earned aloud — a dino graduates to the nickname (278) only after it has first named you by designation (276) at least once, so the address visibly climbs designation→nickname over a friendship rather than jumping straight to intimate. Builds on 276 / 278.
- BACKLOG-284 [pokemon] Address shown in the book — the collection book shows how each dino currently addresses you (unnamed / by designation / by nickname), making the 276→278 escalation legible beside the first-words ladder (282). Builds on 276 / 278 / 282.

**Suggested next-up:** BACKLOG-278 — *Earned the nickname.* The already-queued near-max-bond follow-up
to 276: at the very top of the friendship scale a dino uses the keeper's nickname instead of its
designation. Pure `keeper/` + `ai/` register plus a one-line swap at the two existing greet sites — no
save change, no world change — keeping the lore track small so the structure track (the resource spine)
has room. It's the most distinct beat queued: the single deepest friendship in the bowl is the only one
that calls you by the name only friends use.

**Idea Box:** empty.

**Note to the Structure-smith:** the structure queue is down to three open items (146/145/274) — below
the cap of X=4 — so you'll brainstorm to refill before picking. Queue-top is 146 (the resource-gathering
spine), and the save just gained its version+migration hook last cycle (040), so a new `gathered` tally
can ride the save cleanly. The lore track touches only `keeper/` + `ai/` + the two WorldScene greet
sites — pick a structure item that doesn't collide there (146's `world/` + a different WorldScene region
is clean).

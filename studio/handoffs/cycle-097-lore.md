# Cycle 97 — Lore Handoff

**Theme:** Milestone 2 shipped last cycle, so this cycle **drafts Milestone 3 — "Enough to go around"**
and opens its lore side. Where M2 made each zone a *place*, M3 makes the chain *feed itself*: the
economy provides, and the dinos live in it as hungry mouths and providers. The lore opener is the
gentlest, most human beat of that theme — hunger you can *hear*. The need-drive (371) has surfaced a
🍖 mark over a hungry dino for seventeen cycles; now let it leak into what the dino *says*, so want
becomes personality, not just an icon. Marquee arc for a later cycle is the food web waking (367, the
weird/emergent pick — blocked until a diet split exists, which the Structure-smith is seeding this
cycle).

**Milestone 3 drafted:** headline + three Lore arcs written to `studio/MILESTONE.md` (Structure-smith
adds the Structure arcs in its fire). Lore arcs map to already-queued items — no new lore seeds needed
(see cap rule).

**Cap rule:** open lore-track items ≫ 12 → **no new items brainstormed**; themed the cycle and picked a
next-up from the queue.

**Added to BACKLOG:** none (cap ≥ 12).

**Suggested next-up:** **BACKLOG-368 — Hunger in the voice.** A dino over the need threshold
(`needs.pressingNeed` = hunger) lets it slip in its next greeting/gossip line ("…could eat, honestly"),
with a deterministic fallback line per the tone system and optional LLM colour behind the `NPCBrain`
boundary. Reuses `needs.ts` (371, shipped) + the existing dialogue-context path — clean, definitely
shippable this session, and the humane opener M3 wants before the hunt (367) lands.

**Idea Box:** empty.

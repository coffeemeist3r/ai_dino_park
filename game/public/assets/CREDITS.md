- Sleeping poses, brontosaurus + compsognathus + parasaurolophus (pixel, 20×20 ×2 each) — `game/src/art/pixelArt.ts` (`SUNNY_DOWN` / `COMP_DOWN` / `GLADE_DOWN`) → `bake.ts` (`ensureDown`) → `WorldScene.refreshSleepPoses` — cycle 148-art, 2026-09-02 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-525 closes the set BACKLOG-522 opened: half the shipping cast sleeps every night and three of them did it standing up. Every constraint was already written down as numbers by 522's unit test, so this fire had a bar rather than an opinion — topmost painted row strictly *below* the standing frame's, bottom row *exactly equal* to it (a sleeper whose feet leave its tile is a bug nothing else in this park can see), at least three quarters of the standing mass retained, two frames baked at 1.5fps against the walk's six. **Each keeps its species' silhouette feature upright and settles everything under it**, which is the rule Rex's frill and Mossback's plate ridge established: a sauropod IS its neck, so Sunny's neck folds down along the flank and the head comes to rest at the shoulder rather than disappearing; a hadrosaur with its crest down is a different animal, so Glade's tube crest holds its full sweep and angle while the body settles beneath it. Twitch is the one that curls — the cast's only biped, so the tail comes round and the head tucks toward it, with the two-tone dorsal stripe following the curl because that marking is what keeps a coiled compsognathus from reading as a stone. **The first draft was rejected on a number:** Twitch came out **18% heavier** than its standing frame, and a small biped that gets *bigger* when it lies down is wrong even though the mass floor is only a floor. Redrawn tighter, to 1.02. Reachable on a fresh save with no clock touched: Pip (grove), Thornback (fernreach) and Ember (ridge) are all night-owls, all down at 08:00, and two of the three are the only resident of their ground. **The fallback control moved rather than retiring.** 522 exercised graceful degradation by leaving three species undrawn, and this item has just spent that control; `down` stays optional on `PixelRig` and the unit test now pins the *code path* — a rig with no down pose reports no down art and keeps its standing frame — so a species added tomorrow still renders.

# Asset Credits

All art in AI Dino Park is **Claude-authored procedural vector code** — no image API, no asset
packs, no raster downloads. There are no binary sprite files; the art is the code under
`game/src/art/` (pure shape rigs) baked to Canvas textures at runtime. Logged here per
STYLE-GUIDE.

| Subject | Module | Origin | Date |
|---|---|---|---|
| Triceratops walk loop (Rex) | `game/src/art/dinoArt.ts` → `bake.ts` | Claude-authored procedural vector | 2026-06-03 |
| Brontosaurus walk loop (Sunny) | `game/src/art/dinoArt.ts` (`brontosaurusPose`) → `bake.ts` | Claude-authored procedural vector | 2026-06-04 |
| Parasaurolophus walk loop (Glade) | `game/src/art/dinoArt.ts` (`parasaurolophusPose`) → `bake.ts` | Claude-authored procedural vector | 2026-06-05 |
| Compsognathus walk loop (Twitch) | `game/src/art/dinoArt.ts` (`compsognathusPose`) → `bake.ts` | Claude-authored procedural vector | 2026-06-06 |
| Stegosaurus walk loop (Mossback) | `game/src/art/dinoArt.ts` (`stegosaurusPose`) → `bake.ts` | Claude-authored procedural vector | 2026-06-08 |
| Triceratops pixel walk (Rex, GBA restyle) | `game/src/art/pixelArt.ts` (`REX_RIG`) → `bake.ts` | Claude-authored procedural pixel (CHARTER v4) | 2026-06-09 |
| Stegosaurus pixel walk (Mossback, GBA restyle) | `game/src/art/pixelArt.ts` (`MOSS_RIG`) → `bake.ts` | Claude-authored procedural pixel (CHARTER v4) | 2026-06-10 |
| Brontosaurus pixel walk (Sunny, GBA restyle) | `game/src/art/pixelArt.ts` (`SUNNY_RIG`) → `bake.ts` | Claude-authored procedural pixel (CHARTER v4) | 2026-06-10 |
| Compsognathus pixel walk (Twitch, GBA restyle) | `game/src/art/pixelArt.ts` (`COMP_RIG`) → `bake.ts` | Claude-authored procedural pixel (CHARTER v4) | 2026-06-11 |

- **Glade the parasaurolophus (pixel)** — `game/src/art/pixelArt.ts` `GLADE_RIG`, cycle 044-art, 2026-06-11. Claude-authored procedural pixel (Gen3 style, original pixels). The cast's fifth and final pixel restyle — the tube crest in shared bone tone.

- **AETHER-1 "Aki" keeper avatar (pixel)** — `game/src/art/keeperArt.ts` `AKI_RIG` → `bake.ts` (`makeKeeperArt`), cycle 045-art, 2026-06-12. Claude-authored procedural pixel (Gen3 style, original pixels). First of the robot-observer roster (BACKLOG-158): a 16×20 front-facing brass diplomacy bot — cyan optic visor, antenna + chest glow, stepping legs. VANTA-9 / LUMEN-3 stay on the amber-square fallback until drawn.
- VANTA-9 "Vix" keeper avatar (pixel, 16×20 ×2) — `game/src/art/keeperArt.ts` (`VIX_RIG`) — cycle 046-art, 2026-06-12 — Claude-authored procedural pixel rig.
- LUMEN-3 "Lux" keeper avatar (pixel, 16×20 ×2) — `game/src/art/keeperArt.ts` (`LUX_RIG`) — cycle 047-art, 2026-06-12 — Claude-authored procedural pixel rig. Completes the observer roster (BACKLOG-158 3/3); keeper fallback control re-pointed to a genuine no-art id.
- Grass ground tile (pixel, 16×16 ×2, two variants) — `game/src/art/tileArt.ts` (`GRASS_RIG`) → `bake.ts` (`bakeTileMap`) — cycle 048-art, 2026-06-13 — Claude-authored procedural pixel (Gen3 style, original pixels). The bowl floor, BACKLOG-033 (1/3): the flat two-green checker becomes baked Gen3 grass — a flat field green scattered with darker blade tufts (light tip over a two-pixel stem), two variants alternating like the old checker, every tile's border flat base so it tiles seamlessly. Path + water deferred to the zone that needs them (BACKLOG-143).
- Dialog box frame (pixel 9-slice, 18×18 ×2, slice inset 6) — `game/src/art/frameArt.ts` (`DIALOG_FRAME`) → `bake.ts` (`bakeDialogFrame`) → `ui/DialogBox.ts` (Phaser NineSlice) — cycle 049-art, 2026-06-14 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-036: the flat dark-rect + cream-rect message box becomes a Gen3 box — chunky dark frame line, an inner highlight bevel, cream fill, stepped (2px) rounded corners. Baked once to a 9-slice source and stretched to the box so corners stay crisp; the two flat rects remain as the graceful fallback.
- Resource + cairn props (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`BRANCH_RIG` / `STONE_RIG` / `CAIRN_RIG`) → `bake.ts` (`bakePropArt`) — cycle 066-art, 2026-06-21 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-296: the gathering→craft arc's objects stop being emoji glyphs — a fallen branch (wood log with a pale cut end), a stone (rounded boulder, top-left highlight / bottom-right shadow), and the crafted cairn (three stacked stones, bottom widest). WorldScene swaps `spawnResource`/`drawCairn` to the baked image where a rig exists; the 🪵/🪨/🗿 emoji stay as the graceful fallback. The arc's first renderable-now art (no terrain dependency, unlike the benched path/water -033).
- Path + water ground tiles (pixel, 16×16 ×2, two variants each) — `game/src/art/tileArt.ts` (`PATH_RIG` / `WATER_RIG`) → `bake.ts` (`bakeTerrainMap`) — cycle 067-art, 2026-06-21 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-033 (3/3, completing grass+path+water): the long-benched path/water tiles finally render now that the grove terrain (BACKLOG-294) gives them sub-regions to land in — a warm trodden-earth path (darker divots + upper-left-lit pebbles) along the grove's mid trail, and a mid-blue pond (sparse light/dark horizontal ripple dashes) in the NE corner. Both two-variant with flat-base seamless borders (so each tiles among itself); the pond↔grass boundary is a hard shoreline by design. The flat-checker fallback stays the control (an undrawn ground kind reports `__hasTileArt`→false).
- Plot crop-stage props (pixel, 16×16 ×2, three stages) — `game/src/art/propArt.ts` (`CROP_SEED_RIG` / `CROP_SPROUT_RIG` / `CROP_RIPE_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawPlotSprite` — cycle 070-art, 2026-06-22 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-317: the plantable plot (145) stops rendering its stages as emoji (🌱🌿🍓) — a seeded soil mound (two pale seeds on a brown crown), a leafy sprout (thin stem + two small leaves rising from the mound), and a ripe crop (a full leaf bush dotted with red berries). All three share the same soil mound (o/m/h) so the plot reads as one place growing; the plant rises and reddens stage by stage. The plot sprite swaps to the baked image per stage (recreated only on a stage change), keeping the emoji as the graceful fallback for the empty plot. Renderable now that 308 zone-scoped the plot to the bowl.
- Lean-to shelter prop (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`SHELTER_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawShelter` — cycle 074-art, 2026-06-24 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-344: the dino-built shelter (315) stops rendering as the 🛖 emoji — a lean-to of lashed branches, a single sloped roof rising from a back support post down to a wide front eave, with the open shaded interior tapering underneath. Built of branches, so it shares the branch prop's wood/outline tones (it's the same material, stacked). First draft made the roof a symmetric A-frame that read as a tent, not a lean-to — rev 2 gave it one slope + a back post so the open front reads. WorldScene swaps `drawShelter` to the baked image where the rig exists; the 🛖 emoji stays as the graceful fallback. Renderable now that 315 ships + 308 zone-scopes it.
- Fern scrub ground tile (pixel, 16×16 ×2, two variants) — `game/src/art/tileArt.ts` (`FERN_RIG`) → `bake.ts` (`bakeTerrainMap`) — cycle 086-art, 2026-06-30 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-406 (399's terrain art): The Fernreach's new `fern` ground kind stops baking as grass-under-tint — an olive bracken floor (warmer/yellower than grass's field green) scattered with small fern fronds (a light tip over a two-pixel dark stem, a leafier scatter than grass's single blades), so the third zone reads as fern flats. Two-variant with flat-base seamless borders (tiles among itself); registered in `TILE_RIGS` so `bakeTerrainMap` blits it where `fernreachTileAt` (399) places the scrub. The first renderable terrain art since the grove's path/water (033) — 399 unblocked it the way 294 unblocked 033. The flat-checker fallback stays the control (an undrawn ground kind reports `__hasTileArt`→false).
- Frond resource prop (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`FROND_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.spawnResource` — cycle 089-art, 2026-07-03 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-419: the Fernreach's own resource (the 🌾 frond, BACKLOG-400) stops rendering as a bare emoji glyph like the branch/stone already got under 296 — a golden reed tuft, a fan of stalks arching up from a common base to drooping seed-tips, in warm golds kept clear of the branch's wood-brown and the grass/fern greens so the third zone's gather reads apart. A static single-frame prop (twin of BRANCH_RIG/STONE_RIG), registered in `PROP_RIGS['frond']` so `bakePropArt('frond')` resolves and `spawnResource` swaps the emoji text for the baked image; the 🌾 emoji stays as the graceful fallback. Renderable-now art the cycle-88 Artist flagged; the fallback control still holds (an unrigged kind reports `__hasPropArt`→false).
- Frond thatch prop (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`THATCH_RIG`) → `bake.ts` (`bakePropArt`) — cycle 091-art, 2026-07-04 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-427: the first **stash-ahead** rig (Idea Box rule adopted cycle 91) — the woven frond thatch BACKLOG-417 will raise as the Fernreach's own built landmark, authored ahead of the structure that places it. A cinched vertical reed stack: ragged pale seed-tips fringing the crown, a dark binding cord at the waist, the skirt flaring to a shadowed base course — silhouette held apart from the cairn's stone tiers and the lean-to's slope, in the frond's warm-gold family (built of fronds the way the lean-to is built of branches). First draft was a plain dome that read as a gold boulder beside STONE_RIG at 32px — rev 2 added the crown fringe + waist band + base flare so the stack reads. Registered in `PROP_RIGS['thatch']` so `bakePropArt('thatch')` resolves standalone today; world wiring (STRUCTURE_BY_BIAS frond→thatch + draw) lands with 417. The fallback control still holds (an unrigged kind reports `__hasPropArt`→false).
- Grove greens ripe-crop rig, wired (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`CROP_RIPE_GREENS_RIG`, stashed cycle 095) → `bake.ts` (`bakePropArt`) → `WorldScene.drawPlotSprite` via `plot.ts` `ripeRigKey` — cycle 096-art, 2026-07-09 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-434: the display half of per-zone crops (418). The grove's ripe plot stops falling back to the 🥬 glyph and bakes the greens bush stashed a cycle ahead — a leafy green head, no berries, distinct from the bowl's red-dotted berry bush. `drawPlotSprite` now bakes each crop's *own* ripe rig via the new pure `ripeRigKey(food)` (berries → `crop_ripe` byte-identical, greens → `crop_ripe_greens`, any rig-less crop → its glyph), so the two zones' farming reads apart on the ground, not just in the marker. No new rig authored (it existed); this is the thin wiring that hangs it. The glyph fallback still holds for a crop with no stashed rig (`fish` → no prop).
- Fernreach roots ripe-crop rig (pixel, 16×16) — `game/src/art/propArt.ts` (`CROP_RIPE_ROOTS_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawPlotSprite` via `plot.ts` `ripeRigKey` — cycle 098-art, 2026-07-11 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-432 (art): with the Fernreach plot shipping this cycle, the third zone's ripe crop stops falling back to the 🍠 glyph while the bowl (berries) and grove (greens) already bake pixel rigs. A plump orange tuber shouldering out of the shared soil mound under a small leaf sprig — the crop's bulk is the *root*, not the leaves, so it reads apart from the berry bush and the greens head at a glance, with no berry-red at all. Warm root tones (a starchy orange body + a sun-caught highlight) suit the sunlit fern flats. Registered in `PROP_RIGS['crop_ripe_roots']` so `ripeRigKey('roots')` resolves and `drawPlotSprite` bakes it; the 🍠 glyph stays the graceful fallback for any crop with no rig. Renders standalone via `bakePropArt` — a stash-rule rig authored the same cycle its host plot landed. All three zones' ripe crops now read as distinct pixel props.
- Granary prop (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`GRANARY_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawGranary` — cycle 110-art, 2026-07-24 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-454: the food-cap-lifting granary shipped this cycle as a 🏛️ glyph; this draws it. A broad domed storehouse — a plaster-stone body under a warm timber roof-dome with a dark timber door — reading as a place a zone *stores a bigger surplus*. Its silhouette is held apart from the cairn's stone tiers, the lean-to's slope, and the thatch's cinched stack, and its palette is deliberately **neutral** plaster/timber (not the frond gold, not the branch wood), because unlike the bias landmarks every zone earns the *same* granary — it must not read as any one zone's colour. Registered in `PROP_RIGS['granary']` so `bakePropArt('granary')` resolves and `drawGranary` swaps the emoji text for the baked image; the 🏛️ glyph stays the graceful fallback (`__granaryIsArt` proves the image path). Authored the same cycle 454 landed the structure — renderable-now, not stash-ahead. The fallback control still holds (an unrigged prop reports `__hasPropArt`→false).
- Dropped-food props, fish + berries (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`FISH_RIG` / `BERRIES_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.dropFood` — cycle 135-art, 2026-08-20 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-490: the single most-looked-at object in this park — the thing five dinos sprint at, fight over, cede, gobble, and remember for the rest of the save — was the last prop of consequence still rendering as a font glyph, while the cairn, granary, thatch, lean-to and all three ripe crops baked pixels. A silver fish (side-on, nose left, one dark eye, split tail, cool blue-steel flank under a silver highlight — the curiosity favourite) and a berry cluster (three berries, two low and one perched, one specular pixel each in the GBA manner, a small green sprig above — held clear of the ripe-crop bush's red so a dropped berry and a ripe plot never read as the same object). Keyed **`food_<id>`** rather than as two bespoke props, which is the whole wiring decision: `dropFood` looks a rig up per piece and keeps the emoji for any id not yet drawn, the same graceful per-item fallback `drawPlotSprite` uses for a rig-less crop — so the roster is **deliberately partial** (2 of 7) and the park is entirely fine, with a unit test that fails the day it completes so 490 gets closed rather than forgotten. The fallback control still holds and is now asserted in both directions: a drawn id bakes an Image, an undrawn id (`mushrooms`) stays a Text glyph.
- Egg by the den (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`EGG_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawEgg` — cycle 135-art, 2026-08-20 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-491: the one object in this bowl that turns into a character — a bonded pair nests, lays, and the whole lineage/plaque arc starts here — and it was a 🥚 glyph. A cream speckled shell, top-left highlight falling to a warmer underside, irregular tan speckles scattered so no two rows repeat, over a flat ground-shadow course at the base: the shadow is the point, because an egg drawn without one floats, and this one has been **set down** by somebody. Small subject, high meaning. Registered in `PROP_RIGS['egg']` so `bakePropArt('egg')` resolves and `drawEgg` swaps the text sprite for the baked image; the 🥚 emoji stays the graceful fallback, and `__eggIsArt` proves the image path the way `__granaryIsArt` and `__cairnIsArt` do for theirs.
- Ruin rigs, toppled cairn + caved lean-to (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`CAIRN_RUIN_RIG` / `SHELTER_RUIN_RIG`) → `bake.ts` (`bakeRuinArt`) → `WorldScene.applyObjectVisibility` — cycle 136-art, 2026-08-21 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-494: since cycle 128 a landmark in disrepair has been drawn by turning its **own** sprite down to `DERELICT_ALPHA` (0.45), which was the honest placeholder while nothing was drawn and reads as a cairn *in fog* rather than a cairn that *fell over*. This cycle's structure track (488) makes a fallen cairn in the Grove the first structure any new player walks up to, so it gets a silhouette of its own. **The rule both rigs follow — and the thing the first draft got wrong — is that a ruin is not the same shape, shorter.** A cairn missing its top course just reads as a smaller cairn; what says *this came down* is loose material lying on the ground **beside** the thing, out where nobody stacked it. So the toppled cairn is a squat surviving stub with two stones fallen off, one to each side, lying flat at the ground line, and a unit test asserts the ruin occupies columns the standing rig never uses (as well as starting lower down). The caved lean-to is the same idea in wood: the back post snapped off short, the roof slumped almost flat, and a dark gap punched through the middle where it came in. Both keep their **intact twin's palette** — a ruin is the same stone and the same wood, not a greyer version of them — asserted per-char in the test. Keyed by convention as `<name>_derelict`, so `bakeRuinArt` resolves one without a second registry and the remaining two (thatch, granary) are a rig plus one line. **Deliberately 2 of 4**: the thatch and the granary keep the alpha fade, and the e2e asserts that fallback in both directions, so the graceful-degradation control is live rather than notional.
- Dropped-food props, hunk of meat + leafy greens (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`MEAT_RIG` / `GREENS_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.dropFood` — cycle 137-art, 2026-08-22 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-490 (4 of 7): the two ids at the **top** of the `FOODS` table, and the two the founding cast's favourites resolve to most often, were still font glyphs while the fish and berries beside them in the same hatch baked pixels. Each rig had a first draft that was a plausible picture of the *wrong thing*, and in both cases the fix was one distinguishing feature rather than more detail. **The meat**: a red oval with a pale stripe through it is a chilli at sixteen pixels — what says *meat* is the **knuckle**, a bone with two lobes at its head sticking out of the mass at an angle nothing grows at, and the fat **marbling** through the muscle, because a solid red field reads as fruit and this park drops a red fruit through the same hatch. The unit test pins both: the bone must start above the first muscle row (a bone through the middle is a stripe), and the marbling must span more than one row (one streak is a highlight). **The greens**: this park already draws a fern (419) and a ripe greens crop (418), both of them things growing out of the ground — so what the hatch drops is *cut and bound*, and the **twine** tie at the stem is the entire distinction. Three broad leaves with lit veins, a cord band below every leaf and above a cut stem; the test asserts that order, because a bundle has one. Both keyed `food_<id>` on the 135 convention, so no wiring changed at all — two registry lines, and `dropFood` finds them. **Still deliberately partial (4 of 7)**: roots, mushrooms and seeds keep their emoji, and the e2e exercises the fallback in both directions on every run, so the graceful-degradation control stays live rather than becoming a thing the code claims about itself. The unit test fails the day the roster completes, so 490 gets closed rather than forgotten.
- The ritual's worn ground, two-tile scuff + trodden ring (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`TIC_PACE_RIG` / `TIC_CIRCLE_RIG`) → `bake.ts` (`bakePropArt`), stashed ahead of the `WorldScene` wiring — cycle 138-art, 2026-08-23 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-496 (2 of 3): the tic (405) is the most per-dino behaviour in this park and for fifty cycles it left the ground exactly as it found it — a dino paces the same two tiles a hundred times and the grass is untouched. This cycle's lore track (421) gave the ritual a **haunt** that drifts a tile per stretch, which is the thing that turns a mark on the ground from a smudge into a *path*, so the mark became worth drawing. **The decision that shaped both rigs is that these are the first props in this file worn *into* the ground rather than standing *on* it**, so neither carries the near-black outline every other prop here needs to cut its silhouette out of the grass — a hard dark edge on a bald patch reads as a hole in the world. The edge is trodden grass, a shade darker and browner than `GRASS_RIG`'s field green, which is what a real worn patch actually fades through; the generic outline assertion in `cycle-066-propart` is skipped for `tic_*` with the reason written down, and replaced by an assertion in `cycle-138-ticground-art` that no colour in either palette is anywhere near black, so neither rig can quietly grow one back. **The scuff**: the first draft was one long oval, which at sixteen pixels is a shadow. What says *pacing* is that the ground is bare at the two **ends** and only trodden thin between them, because the ends are where the animal turns around — the test pins that the mark is wider than it is tall and that one row carries two disjoint bare runs with nothing but track between them. **The ring**: the first draft was a worn disc, which is just a bigger scuff. The whole distinction is the **hole** — an animal turning on the spot wears the circumference and leaves the middle standing — so the test asserts the four centre cells are untouched and that the mark is as wide as it is tall. Keyed `tic_<TicKind>` so a caller looks one up per kind and draws nothing for a kind with no rig. **Deliberately 2 of 3**: `fuss` stays undrawn and the e2e asserts it reports false, so the per-kind fallback stays a control somebody exercises rather than a claim the code makes about itself.
- Ruin rigs, unravelled thatch + cracked granary (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`THATCH_RUIN_RIG` / `GRANARY_RUIN_RIG`) → `bake.ts` (`bakeRuinArt`) → `WorldScene.applyObjectVisibility` — cycle 139-art, 2026-08-24 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-494 (**4 of 4 — the set closes**): cycle 136 drew the toppled cairn and the caved lean-to and left the thatch and the granary on 480's `DERELICT_ALPHA` fade, which reads as fog rather than ruin. Both now have a fallen twin, and **`WorldScene` needed no wiring at all** — the swap is one generic line, `hasPropArt(`${prop}_derelict`)`, so drawing a ruin has been a rig plus a registry entry since the convention was chosen. Both rigs obey the rule cycle 136 wrote down (*a ruin is not the same shape, shorter*) and both first drafts broke it in the same way, by drawing a smaller version of the intact thing. **The thatch**: a reed stack is held up by the cord cinched round its waist, so what happens when it fails is not that the stack shrinks — it **bursts**. A short stub still stands where the binding was tied lowest, the rest of the bundle has slumped sideways, and loose reeds lie flat on the ground out past *both* edges, in columns nothing woven ever occupied. **The granary**: a dome does not lean, it **caves**, so the ruin is a dark seam torn straight down the middle of the body with two stubs of roof timber left standing either side of it and plaster chunks fallen out to both edges. The seam deliberately runs the whole body rather than stopping at the door, because what makes a granary a ruin is that the thing it existed to keep dry is not dry any more — and it is drawn in the doorway's own shadow colour, so the building reads as opened rather than repainted. Both keep their intact twin's palette (a ruin is the same plaster and the same reed, not a greyer version), asserted per-char. `cycle-139-ruin-art.test.ts` generalises cycle 136's cairn-only silhouette assertions to **every** registered ruin — spills into unused columns, starts lower than the standing crown — so the fifth landmark this park builds cannot skip them. **And the fallback control moves rather than retiring**: with all four landmarks drawn, the e2e now rides it on a name nobody has drawn a fallen twin for at all, so "a landmark with no ruin keeps the fade" stays a path somebody exercises instead of a claim the code makes about itself.
- Dropped-food props, starchy roots + pale mushrooms + hard pine seeds (pixel, 16×16 ×2) — `game/src/art/propArt.ts` (`FOOD_ROOTS_RIG` / `FOOD_MUSHROOMS_RIG` / `FOOD_SEEDS_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.dropFood` — cycle 140-art, 2026-08-25 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-490 (**7 of 7 — the food roster closes**): three fires and the last three glyphs are gone, so every piece of food the hatch drops — the thing the cast sprints at, fights over, cedes, gobbles and remembers for the rest of the save — now bakes a real prop. Each rig had a first draft that was a plausible picture of something this park already draws, and in each case the fix was one distinguishing feature rather than more detail. **The roots**: the first draft was `crop_ripe_roots` (432) again, smaller — a tuber shouldering out of a soil mound under a leaf sprig. But *what the hatch drops is cut*, so there is no soil and no sprig: two scrubbed tubers, each wearing the pale scar of a **cropped crown** where the leaves came off, with a taproot whisker trailing from each tail. Two of them, not one, so the silhouette is a pair of tapers rather than a lump that could be anything. **The mushrooms**: the first draft was one big spotted toadstool, wrong twice — a single cap at sixteen pixels is a lollipop, and a red one is a berry with a stick in it, and this hatch already drops berries. The read is the **cluster** and the **gills**: three caps at different heights give a lumpy outline nothing else in `PROP_RIGS` has, and a dark band under each cap says the underside is open, which is the one thing a mushroom has that no fruit does. Cool and pale on purpose — everything else in the hatch is warm, so this is the odd one in the pile at a glance. **The seeds**: a **heap**, not an object. Every other food is one thing with one silhouette; this is the only one that is many, and leaning into that is what stops it reading as a small brown berry. Six facetted nuts stacked three-two-one, each with a single lit facet, two of them **cracked**, because the label is "hard pine seeds" and a crack is the cheapest way to say a thing has a shell. All three keyed `food_<id>` on the cycle-135 convention, so **no wiring changed at all** — three registry lines and `dropFood` finds them. **And the fallback control moves rather than retiring.** 490 shipped for three fires against a `drawn < FOODS.length` guard that said, in both the unit test and the e2e, "when this fails, 490 is complete: close it." It failed; 490 is closed. The assertions are inverted rather than deleted, so the fires that got here stay legible (2 at cycle 135, 4 at 137, 7 at 140), and the graceful path now rides on an id the park has never dropped — so "a food with no rig keeps its emoji" stays something somebody exercises rather than a claim the code makes about itself, and it can no longer be closed out from under by finishing a roster.
- The ground's bank at three fullness steps (pixel, 16×16 ×3) — `game/src/art/propArt.ts` (`PILE_1_RIG` / `PILE_2_RIG` / `PILE_3_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.syncBank` — cycle 141-art, 2026-08-26 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-506, drawn for BACKLOG-504 **in the same cycle that placed it**, which is not how this queue usually runs: the structure track shipped the bank tile and the step function on the stone-glyph fallback that morning, so the rigs land on wiring that already exists and the graceful path is what shipped first rather than what was promised. **The problem this set had to solve is that the park already draws stacked stone, and it is sixteen pixels away.** The cairn (296) is *built*: level courses, a clean taper, every stone squared to the one beneath it. A bank is *piled*, so it gets the inverse of all three — lumps at staggered heights whose outlines deliberately never line up into a course, a crown perched off-centre, and at the full step a stone that has plainly rolled clear of the mass. The unit test pins that as an invariant rather than a hope: no step may contain a full-width run of pure outline, which is precisely the cairn's tell. **The first draft failed on the requirement the item actually stated** — legible in *silhouette* from across a ground — because step 1's two scattered pebbles were flung so far apart that their bounding box was as wide as the shouldered heap of step 2. Detail cannot separate three states at sixteen pixels; outline can. So the scatter was pulled tight, and the test now asserts each step is strictly taller *and* wider *and* denser than the one below, which is what lets a keeper standing on the Grove tell a full bank from a spent one without opening the lens. **One detail carries the meaning past 'these are rocks'**: only the full step wears wood — a branch end shouldering out of the heap — because a bank holds whatever the ground gathered, and it is the one step with room to say so. Park earth tones throughout (the loose stone's own outline, body and faces), never the keeper palette: this is a heap dinos made. Keyed `pile_<step>`, so `pileArtKey` looks one up by convention; **there is deliberately no `pile_0`**, because step 0 is bare ground rather than a picture of bare ground, and the e2e asserts that absence. The swap path is the thing the e2e really exercises: `syncBank` drops and re-creates its sprite when a step crosses between the glyph fallback and a baked rig — the guard against a half-drawn rig set calling `setTexture` on a `Text` — and with steps 1–3 drawn and step 0 not, every save now crosses that seam the first time a ground banks anything, so it is exercised on every boot instead of sitting dormant.
- The turned-over patch, the black glass, and the thing set from it (pixel, 16x16 x3) — `game/src/art/propArt.ts` (`TIC_FUSS_RIG` / `OBSIDIAN_RIG` / `BEACON_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.syncWear` / `spawnResource` / `drawBeacon` — cycle 142-art, 2026-08-27 — Claude-authored procedural pixel (Gen3 style, original pixels). Three rigs, two items, and both of them closed the same night the code that needs them shipped. **BACKLOG-496 (3 of 3 — the worn ground closes).** `fuss` had been held back for four cycles on purpose, as the per-kind fallback control; this cycle's lore track (507) finally gave the marks a *host*, so the item became worth finishing rather than worth protecting. The rig's whole problem is that its two siblings already occupy the obvious shapes: the scuff is two bare patches with a track between them, the ring is a circumference with the middle left standing, and both of those are **wear** — grass rubbed off by a repeated line of travel. `fuss` is not wear at all. An animal that stays on one spot and picks at it *turns the ground over*, so this is the only one of the three with no hole and no smooth rim: one compact clump of loose earth with clods sitting proud of it, its outline deliberately ragged where the other two are smooth. The first draft was a small filled scuff, which is to say `tic_pace` with one patch instead of two — the same picture at sixteen pixels. The three separate in **silhouette**, because a player reads them from across a ground: two blobs, a ring, one ragged clump. `cycle-138-ticground-art.test.ts` pins each of those: no row of `fuss` may carry two disjoint runs of earth (that is `pace`'s tell), its centre must be turned over (a hole is `circle`'s tell), and it must be roughly square and smaller than either — a spot, not a route. All three share one palette, so the park speaks one worn-ground language rather than three. **BACKLOG-508 (the Ridge's black glass, drawn for BACKLOG-503).** The obsidian is the first prop in this file whose body is genuinely dark, and that **inverts a rule the whole set has obeyed since 296**: every other rig sits inside a near-black outline, which is invisible around a near-black shard on dark grass. So the obsidian keeps the outline char `o` that every prop uses and makes it the *lightest* colour in its palette — the one rig in the table where that is true, and written down in the source as such. Silhouette carries the rest: a stone (296) is a rounded lump, and a black lump is a stone somebody painted, so volcanic glass is drawn as an **angular splinter** — every edge straight, no two the same length, the long axis leaning off vertical — with a single specular glint on the fracture face, which is the only thing that says *glass* rather than *coal*. The beacon had the harder job, because it has two things to be distinct from at sixteen pixels: the cairn, which is stacked stone, and the bank heap (506), which is dropped stone. The first draft stacked shards the way the cairn stacks stones and was simply a black cairn. The fix is that **nothing here rests on anything** — three splinters stand on end in a set base at different heights and leaning apart, breaking the top of the box, because the whole point of a thing on a ridge is that it is the skyline you can see is different from a ground away. The base is ordinary park stone with the ordinary dark outline, so the glass reads as the thing that was *brought*. **And the fallback control moved twice in one night, which is the fire's real finding.** Two e2e specs (089, 091) had pinned "a key with no rig reports false" to the literal string `'obsidian'` — a plausible-sounding thing nobody had drawn — and this cycle made obsidian a real resource and then drew it; a third (138) had pinned it to `tic_fuss`, closed above. The control now has a name nothing can ever claim, declared once beside the registry it guards (`NO_RIG_CONTROL`), so the graceful path stays something somebody exercises rather than a claim that quietly expires the next time the park invents a word.
- The feeding hatch and the salt crust (pixel, 16x16 x2) — `game/src/art/propArt.ts` (`HATCH_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.drawHatch`, and `game/src/art/tileArt.ts` (`SALT_RIG`) → `bake.ts` floor bake — cycle 143-art, 2026-08-28 — Claude-authored procedural pixel (Gen3 style, original pixels). Two rigs, two items, both closed the same night the code that needs them shipped — the third fire running on that cadence. **BACKLOG-502 (the feeding hatch — the last undrawn prop key in the park).** `H` is the most-pressed key in this game and until this cycle the thing it opens did not exist on screen at all; the lore track (510) put a hatch tile on every ground that morning, on the glyph fallback, so this rig lands on wiring that already runs. Its whole problem is that it is the **odd artefact**: every other prop in this file is dino-made or grown, and shares one grammar — organic outline, earth palette, lit at the top-left and shadowed at the bottom, the way an object standing on grass is lit. The hatch is keeper-made and set *into* the ground, so it is drawn against all three. It is **rectilinear**, because round is what animals dig — the first draft was a bevelled ellipse and at sixteen pixels that is a burrow with a metal rim. It is **cool steel**, blue-shifted in every channel, where the house rule since 296 is a warm brown-olive outline; the unit test asserts that per colour, so it cannot drift back into the earth palette. And the load-bearing decision is the **lighting inversion**: shadow on the upper inner wall, light on the lower one, which is the opposite of every rig in this file and the only thing that says *hole* rather than *plate lying on the grass*. That inversion is exactly the kind of thing a later tidy-up pass would 'correct', so it is a test rather than a comment. The shutter is two leaves parked either side of the mouth — the one detail that says the hole opens and closes instead of simply being a hole — and the mouth holds the darkest value in the park, because nothing in it is lit. **BACKLOG-511 (the salt crust — the Saltpan's floor).** The structure track laid a sixth ground the same morning with a new `salt` tile kind on the flat-checker fallback, so this is the first terrain rig in the park's history whose host shipped in the *same cycle* as the request; the cycle-91 stash rule never came into play. Its first draft failed on the one thing that mattered: every ground kind in `tileArt.ts` is a base colour with two-pixel marks scattered over it — grass has tufts, fern has fronds — and salt drawn that way is a paler lawn. **A crust is not scattered, it is broken.** So the marks are closed irregular polygons whose edges meet, and the eye reads plates rather than noise; the test measures the largest connected run of crack pixels and fails if the cracks fall back into speckle. Three plates per variant with a lifted edge or two catching the sun, the palest ground values in the park, and no green cast at all — asserted against `GRASS_RIG`'s own base colour rather than against a number, so it stays the anti-grass whatever the grass becomes. Borders stay flat base, so the SEAMLESS guarantee holds and the plates simply read as bigger than one tile. **Nothing was left on fallback that had a rig to lose**: with the hatch drawn, the prop registry has no undrawn key at all, and the draw-a-rig-or-draw-nothing control stays on `NO_RIG_CONTROL` — the name declared last cycle precisely because finishing a roster kept retiring the control by accident. For terrain, the control is now `salt`'s absent sibling rather than `salt`, and `cycle-143-salt-tile` says so.
- The founder's stake and the stake that outlived its ground (pixel, 16x16 x2) — `game/src/art/propArt.ts` (`FOUNDER_STAKE_RIG` / `FOUNDER_STAKE_HOLLOWED_RIG`) → `bake.ts` (`bakePropArt`) — cycle 144-art, 2026-08-29 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-513 / 514, and the first fire since cycle 90 to open on an **empty** art queue: every prop key and all four terrain kinds were drawn, so these two were seeded that morning rather than drained, and they were seeded at the thing the night's structure track was about to make true. BACKLOG-512 gave every ground the roster wakes on a recorded founder; the park had no object for a founding. **This is the first prop in the file that is purely a claim** — not grown, not gathered, not built to hold anything. A post driven into the ground with a cross-piece lashed to it, which is what people have always made when they wanted somewhere to say *this is ours* and had only wood. The first draft was a plain post, and the park already has a branch rig: a stake that is only a stick is that rig rotated. The cross-piece is what makes it a **sign** — two axes crossing is the oldest mark there is and the only detail that survives 32px — and the binding at the crossing is the second, because rope says somebody tied it, which is the whole difference between a marker and driftwood. **The pair is the deliverable, not either rig.** The hollowed twin is the same object with exactly three things changed and nothing else: it **cants**, its binding has gone **slack**, and the ground has **crept up the shaft** — so one glyph reads *somebody got here first* or *and then they left* depending only on which variant is baked, and BACKLOG-512's hollowed ground becomes legible from the map without opening a lens. Its first draft leaned by drawing the post crooked in place, which reads as a badly-drawn upright; a lean has to **travel**, so the top of the post now sits four columns off its base and the eye takes the diagonal before it takes anything else. The wood is **desaturated rather than darkened** — the same brightness, almost none of the hue, because a ground everybody left is not a night scene — and the creep is the only green in either palette, asserted per colour so neither rig can grow a second one. `cycle-144-founder-stake.test.ts` pins each of those as a number: one column for every post row on the upright (a post that wanders reads as a stick somebody leaned there), a strictly monotone cant on the hollowed one (a post that wanders back is a wobble, not a lean), strictly less cord showing, saturation less than half its twin's, and creep only in the bottom third. Both share the outline colour, because this is one object in two states rather than two objects. **Stashed ahead of the tile that plants them**, per the cycle-91 rule, on its own condition: both resolve standalone through `bakePropArt`, and the e2e asserts that alongside `NO_RIG_CONTROL` still reporting false, so the draw-a-rig-or-draw-nothing fallback stays a path somebody exercises.
- The stake of a ground nobody walked to (pixel, 16x16) — `game/src/art/propArt.ts` (`FOUNDER_STAKE_NATIVE_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.syncStakes` — cycle 145-art, 2026-08-30 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-517, and the **first rig in this file's history that was wired into the world in the same hour it was drawn, because the park would otherwise have failed a test**. The reachability register (501) shipped that night with an entry nobody had ever written down — *is every rig the studio has drawn a rig the park can actually put on the ground?* — and its first walk went dark on 513 and 514, drawn the night before under the cycle-91 stash rule and placed by nothing. The structure track planted them; this rig therefore had nowhere to hide, and the stash rule's open end is closed for good: a drawn rig with no host is now a red build. **The subject is 516 made into an object.** That item taught the park to say, in the collection book, whether a ground's founder *woke up there* or *walked in*; 513 draws a post **driven** — carried here and hammered in — which is exactly right for the second and exactly wrong for the five grounds the roster wakes on. So the third state is the same post **set**: standing in a ring of laid stones rather than ending in bare dirt, because laid stone is the oldest way of saying somebody meant to stay, with **growth** coming up between them, which is the tell 513 can never have — a driven stake is new and nothing has had time to grow around it. The wood keeps its saturation against 514's bleach: this is a mark being kept, not one being left. **The first draft put the stones in a neat symmetrical arc and it read as a cairn with a stick in it** — the park already has a cairn rig, and a cairn wearing a post is that rig plus noise. The stones came down to a low flanking pair with the post's own base still visible between them, so the post stays the subject and the stone is the ground it was set into. `cycle-145-native-stake.test.ts` pins every distinguishing mark as a number rather than a sentence: a single post column top to bottom (a set post does not lean, which is precisely 514's tell), the same cord showing as 513 and more than 514, stone only in the bottom four rows, a green more saturated than the hollowed creep, and a shared outline with both siblings — one object in three states, not three objects. **Four readings from one tile now**: set (born here), driven (walked in and claimed it), canted and bleached (and then everybody left), and nothing at all, which is the Saltpan and needs no rig, because bare is what unclaimed ground looks like.
- The hours a dino keeps, shut and open (pixel, 16x16 x2) — `game/src/art/propArt.ts` (`DOZE_RIG` / `ROUSE_RIG`) → `bake.ts` (`bakePropArt`) → `WorldScene.refreshSleepMarks` / `refreshRouseMarks` — cycle 146-art, 2026-08-31 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-520, drawn for BACKLOG-109 **in the same cycle that seeded it and the same cycle that shipped its host** — seeded that morning with the host named and dated, per the amendment cycle 145 wrote into the stash rule, and wired that evening. Every prop in this file until now sits on the ground; these two hang over a dino, which makes them the first marks in the set whose job is to say what a body is *doing* rather than what a place *holds*. **The first draft was a `Zzz`, and it was wrong for a reason worth keeping.** Sleepy-Zs are a typographic convention borrowed from comics, and nothing else in this park is typography — every other mark here is an object or a wearing-away of the ground, so a `Zzz` beside `tic_pace` reads as a different game leaking in. It also has no opposite: there is no anti-Z meaning *awake*, so the pair would have had to be a Z and an eye, which is exactly the two-unrelated-symbols failure the brief rules out. The whole requirement is that these read as **one fact at opposite ends**, not as two symbols that happen to arrive together. So both ends are an **eye**, and the axis is whether it is shut — one silhouette, one outline, and a difference a player reads without being taught it. `doze` is the heavy end: a lid closed over the whole eye, lashes laid down beneath it, and one slow breath drifting up and away. `rouse` is the same eye open, with a pupil dark enough to carry a **catchlight** sitting upper-left rather than centred, because a centred one reads as a hole and an offset one reads as wet. **The second draft was caught by its own unit test, which is the better half of the story.** The doze had been drawn with a brown-*black* outline and a near-white breath — a high-contrast mark, and a high-contrast mark does not recede, when the whole job of a sleeping dino is to be the thing your eye slides off. Its ramp now lifts off black and stops short of white, and `cycle-146-hours.test.ts` pins the relationship as a number rather than a sentence: `rouse` must span more than twice the luminance range `doze` does, and no pixel in `doze` may be brighter than `rouse`'s sclera. The pair is separated by **temperature** as well as by value — brown-black outline against blue-black — so it still reads apart for a player who cannot rely on brightness alone, and the test asserts the sign of blue-minus-red on each. The axis itself is pinned too: `doze` must carry a lid and no pupil, `rouse` a pupil and no lid, so a later repaint cannot quietly give them the same interior and leave the park with two marks that mean nothing in particular. **Nothing was stashed.** Both keys went into `worldPlacedProps()` in the same commit that drew them, and the two mark slots swapped from emoji `Text` to baked `Image` on the same per-item fallback `dropFood` and `drawPlotSprite` already use — so the register's ninth entry stayed green from the first walk rather than going dark and being repaired, which is the first time that has been true since the entry existed.
- The dino asleep, in two silhouettes (pixel, 20x20 x2 each) — `game/src/art/pixelArt.ts` (`REX_DOWN` / `MOSS_DOWN`) → `bake.ts` (`ensureDown`) → `Dino.setAsleep` / `WorldScene.refreshSleepPoses` — cycle 147-art, 2026-09-01 — Claude-authored procedural pixel (Gen3 style, original pixels). BACKLOG-522, seeded that morning with its host named and dated and drawn the same night — the second fire running on the cadence the cycle-145 amendment asked for, and the reason it was possible is that **the host was already on screen**: BACKLOG-109's `isResting` has put a dino down out in the open at 08:00 on a fresh save since last cycle, and 307 gave it a voice that morning. **This is the first rig in the park that is not a new object but a second pose of an old one**, and that changes what the brief is. Every other entry in this file had to be distinct from its neighbours; these two have to be *the same animal*, or the swap reads as one dino replaced by another. So the test is a mass floor rather than a silhouette rule: a down pose keeps more than three quarters of its standing frame's painted pixels, because a pose that sheds a third of itself is a blob wearing the right palette. **The first draft was the walk frame with the legs deleted, and it read as a dino sunk into the ground to the knee.** Legs do not vanish when an animal lies down, they *fold* — so both rigs keep a short tucked stub either side under the belly, which is what says the limbs are still there and gathered in. The second thing that draft got wrong is the one the pose exists for: it kept the head where the walk frame had it, and an animal with a lowered body and a level head is not asleep, it is crouching. The whole body drops, head included, and the unit test pins both halves of that as numbers — the topmost painted row must sit **below** the standing frame's, and the bottom row must be **exactly equal** to it, because a sprite whose feet leave the tile it is standing on is a bug nothing else in this park could see. **Two frames, deliberately, where every walk rig has three.** A sleeping animal is a shape that breathes, and a three-beat amble played slowly is precisely the read this pose was drawn to remove; `ensureDown` bakes at 1.5fps against the walk's 6. What moves is the belly band lifting a row and the flank filling back under it — nothing else, so the eye registers it as volume rather than motion. **Per species, the thing that had to survive the fold.** Rex is the frill and the brow horns, so the head stays turned toward the camera at full height off the shoulder and only the legs and the ground line change; its eye becomes a shut two-pixel slit in the same cell the open eye occupied, which is the cheapest possible way to say *asleep* and the only one that works at 20px. Mossback is the staggered double-row plate ridge and nothing else — a stegosaurus with its plates down is a different animal — so the ridge stays upright and completely untouched while everything beneath it settles, and the eye shuts the same way. Both keep their walk rig's exact palette, asserted per char, so a sleeping dino cannot smuggle in a colour its waking self does not have. **Three species are left undrawn on purpose and the unit test asserts that they are.** `down` is optional on `PixelRig`, and a species without one keeps its standing frame while it rests — the graceful path this park has shipped on since 296, and here it is not a claim the code makes about itself but a thing three of five dinos do on every save. The pose is a texture swap on an existing sprite (`setAsleep` flips only on the state edge), never a second rendering path, so the rectangle fallback and the vector rigs are untouched. **And it is posed before the first frame is shown**, in `create()` beside `__ready`, because "frame one" is where this item's read lives: without that, the park opens with its one sleeping dino ambling on the spot for a world step, which is exactly what the rig exists to stop.
- The thought about somebody who was not here (pixel, 16x16) - `game/src/art/propArt.ts` (`MISSED_RIG`) -> `bake.ts` (`bakePropArt`) -> `WorldScene.refreshMissedMarks` - cycle 150-art, 2026-09-04 - Claude-authored procedural pixel (Gen3 style, original pixels). **BACKLOG-531**, seeded that morning with its host named and dated and drawn the same night - the second consecutive fire on that cadence, and the queue's cycle-145 amendment holding for a sixth cycle: 518, whose driver is still unchosen, was held again. This is the **fourth mark on 520's axis and the first that is not an eye**. The three before it are facts about the dino's own hour - shut, open, aimed at you - and this is a fact about the *keeper*: a thought a dino holds about somebody who was not here, worn for a few seconds when they come back. **The first draft was rejected for being an eye.** It drew a third eye variant with the pupil rolled up and away, on the reasoning that a family should look like a family; at 32px that is indistinguishable from `rouse`, and the park would have shipped two marks that mean opposite things and look identical. The family claim is carried by the **colour** - `rouse`'s outline and sclera taken verbatim rather than approximated, because a shared constant is the cheapest way to keep a family claim true rather than merely stated - and by the slot. The silhouette has to differ, because the meaning does. So it is the emoji's own vocabulary: a puff with a tail of two smaller marks rising to it from one side, which is the only pixel grammar at this size that says *about something absent* rather than *awake*. The tail is offset rather than centred, so it reads as a trail coming from the dino instead of a stalk holding the puff up, and the unit test pins that as a number - every tail row below every body row, each narrower than the widest, and the tail's left edge moving between rows. **64 lit cells makes this the lightest mark in the park**, under `doze`'s 67 and `vigil`'s 76, and the order is deliberate rather than incidental: it is the faintest thing a dino can wear, half the dinos wearing one wear it at `MISSED_FAINT_ALPHA`, and a thought somebody is trying not to have should not out-shout a dino asleep. The catchlight steps down the same ladder - dimmer than `vigil`'s, which is dimmer than `rouse`'s, which stays the brightest pixel in the park's hour-marks - and the spec asserts the two inequalities together, so the four marks cannot drift out of their own order. **The register caught it, as designed.** The first run after the rig landed turned six unit tests red across four files: `unplacedRigs()` reported `missed` drawn with no host, and BACKLOG-501's own entry went dark with it. That is the cycle-145 amendment working exactly as written - a drawn rig with no host is a red build - and the repair was one line in `worldPlacedProps()`, the same one `doze`, `rouse` and `vigil` each needed. Nothing was deleted to make it green.
- The beacon that went out (pixel, 16x16) - `game/src/art/propArt.ts` (`BEACON_RUIN_RIG`) -> `bake.ts` (`bakeRuinArt`) -> `WorldScene.applyObjectVisibility`'s `showLandmarks` - cycle 151-art, 2026-09-05 - Claude-authored procedural pixel (Gen3 style, original pixels). **BACKLOG-532**, seeded that morning and drawn the same night, the third consecutive fire on the cadence the cycle-145 amendment asked for. It is the **fifth and last landmark to draw its own ruin**: cycle 139 closed the family at four of five and left the beacon behind, so for twelve cycles a derelict beacon was the lit rig at `DERELICT_ALPHA` - and on the Ridge, a ground made of black glass, dim reads as **night** rather than as abandoned. The wrong sentence, told by the only landmark still telling it. **This rig needed no wiring at all, which is the rarest entry in this file.** `showLandmarks(this.beaconSprites, this.beacons, 'beacon')` has routed through `bakeRuinArt` since BACKLOG-503 and fell back to alpha for one reason: the key was absent. Drawing the rig *was* the wiring. **The first draft was the standing rig with the glints deleted and one shard shortened, and it was rejected for being the alpha fade re-drawn in pixels** - same silhouette, less light, which is precisely the failure the item was opened over. The fix came from asking what the beacon actually is. It is the one landmark in the park that goes **up**: three splinters standing on end in a set base, breaking the top of the box, the skyline you can tell apart from a ground away. Kill the skyline and everything is said. So the tall middle shard is **gone below the base line**, leaving a dark empty socket in the stone; the right one lies fallen against the base, tapered so it reads as *down* rather than as a block; and only a snapped stub still stands, at half height, breaking nothing. **Six rows of the sixteen that the standing rig fills are now empty air**, and that is the whole read at 32px. **No `g` anywhere**, and that is the second half. The glints were the only entry in the palette that said *lit* - the reason, per `BEACON_RIG`'s own header, that you could see it from a ground away - so a beacon that has gone out is the one prop in this park entitled to zero catchlights. Everything else is `BEACON_RIG.palette` **spread with the glint filtered out** rather than re-typed, so the family claim is a shared object rather than a matching pair of literals, the same discipline `MISSED_RIG` took from `rouse`. `cycle-139-ruin-art.test.ts`'s generic rules - shared palette, spills into columns the standing rig never used, crown starts lower - now run over five ruins instead of four and passed the new rig unmodified; its inventory line was amended from four to five, and `cycle-066-propart.test.ts`'s registry list with it.

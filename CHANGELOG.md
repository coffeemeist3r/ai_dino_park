# Changelog

Append-only. Validator adds an entry on APPROVED verdicts. Format:

```
## Cycle NNN — YYYY-MM-DD
- BACKLOG-NNN: <title> — <one-line outcome>
```

---

## Cycle 103 — 2026-07-16 (Milestone 5 "No one goes hungry" opens)
- BACKLOG-373: Shared meal — the trough turns social. Feeding was a scramble (cycle 25): whoever reached a drop ate alone. Now two *different* dinos eating within a short wall-clock window (`SHARED_MEAL_MS=4000`) shared a meal — a two-line pure `sharedMeal(prev, name, at, window)` in `world/feeding.ts` gates on a different eater inside the window; WorldScene holds a transient `lastMeal` anchor (re-set on every meal) and, on a shared meal, applies a small symmetric `strengthen(+SHARED_MEAL_BOND=3)` (below a meet, below the 375 yield's 5), an "ate alongside <other>" memory to each, a 🍽 flash, and a `🍽 <a> and <b> ate together` ticker line. Emergent from the existing eat path; no new saved state (the moment is live). A hatch drop is one piece eaten by one dino, so "same hatch drop" is realized as successive rushes within the window. +4 unit, +2 e2e. **Milestone 5 lore arc 1 ✅.** (lore track)
- BACKLOG-446: A zone banks its harvest — the milestone's missing noun. A harvest used to drop into the feeding loop and vanish, so the demand read (438) pointed at nothing and a carrier (444) had nothing to ferry. New pure `world/foodstore.ts` — the food twin of `resource.ts`'s Stockpile: `FoodPile` (foodId→count), `bankFood` clamped at `FOOD_STOCKPILE_CAP=6`, `foodPileTotal`, `foodPileLine` (`🍓 2 · 🥬 1`, FOODS order). `harvest()` banks one crop unit of the zone's yield into a per-zone `foodPileByZone` (the drop into the feeding loop unchanged — the bank is the stored surplus), read on the zone-map lens as a `🍓 N` line (`ZoneMapEntry.banked`, `zoneMapModel` trailing `foodPiles` arg, boxH 78→92). Additive save field `foodPileByZone` (pre-446 saves load `{}`). The spine 444 (spend on the hungry) / 447 (ferry) / 438 (demand) all stand on. +5 unit, +1 e2e. **Milestone 5 structure arc 1 ✅.** (structure track)

## Cycle 100 — 2026-07-13 (Milestone 4 "The hunt has weight" opens)
- BACKLOG-437: The hunt feeds — the food web that woke in cycle 99 always came up empty; now a stalk occasionally lands, deathlessly, and feeds the hunter. A new pure `huntSucceeds(roll, chance=0.3)` in `world/foodweb.ts` forks the `huntCaught` moment: the quarry **always** slips away (💨 + "you slipped X's hunt" memory, hoisted above the fork with the 30s cooldown, so the prey is unharmed and the cooldown is shared) — only the hunter's luck differs. ~30% the stalk lands (`satisfy(needs, hunter, 'hunger')` resolves the hunger through hunting, `🍖` tell, `🦖 <hunter> made its catch — a lean meal` event, "you brought down a meal" memory); ~70% the cycle-99 empty path, byte-unchanged. Deathless by construction — no roster mutation, no ground FOODS spawn (the take is modelled as a direct `satisfy`, ponytail-noted; a real drop only if another dino should be able to steal it), mortality still the operator's CHARTER call. Hunting is now a real food source for the bowl's lone carnivore, not only the keeper's hatch. No save change. +2 unit tests. **Milestone 4 structure arc 1 ✅.** (structure track)
- BACKLOG-440: Rattled after the chase — the food web reaches the dialog box. When Twitch stalks and a herbivore bolts (367), the prey files `you slipped <hunter>'s hunt`; while that memory is fresh (it ages out of `recall`'s 6-slot window on its own), the prey's next greeting to the keeper leans shaken and **names its chaser** — "…give me a sec, Twitch nearly had me." A new pure `recentHunter(memories)` reads the chaser back out of the memory; `ai/brain.ts` gains `NPCContext.rattled` + `rattledAside(hunter, traits)`, composed onto the base greeting exactly like the hunger tell (368) and temperament-shaded (a prickly dino plays it down, a warm one makes a breathless thing of it, both compose when a dino is hungry *and* rattled). Deterministic fallback carries the AC; the WebLLM prompt gets a parity hint behind the NPCBrain boundary. `rattled` unset ⇒ byte-identical greeting (back-compat). No save change (the 367 memory is the trace). +5 unit tests. **Milestone 4 lore arc 1 ✅.** (lore track)
- BACKLOG-439 [infra] CLOSED — already satisfied. The premise ("the repo tracks no test files") was a **cycle-99 error**: 281 test files were committed (last touched cycle 098-art), `playwright.config.ts` exists, and `test`/`test:e2e` were already wired. The genuine remnant was that the root `vitest.config.ts` include (`tests/unit/**` only) silently **skipped** the colocated `game/src/**/*.test.ts` (cycle-99's diet/foodweb/lenses, 16 tests); broadening the glob this cycle brought the run to 127 files / 1130 tests, all green — the harness now enforces the whole committed suite.

## Cycle 099 — 2026-07-12 (Milestone 3 "Enough to go around" SHIPPED 🎉)
- BACKLOG-367 (+ BACKLOG-435): The food web wakes — the bowl's first hunt, deathless. For ninety-nine cycles nothing hunted anything: food only ever fell from the keeper's hatch or grew in a plot. Now a **diet** splits the cast (435, folded in as this arc's data spine) — a pure `world/diet.ts` keys diet off species by paleobiology (`SPECIES_DIET`: compsognathus → carnivore, the four big grazers → herbivore; unknown/bred species resolve by a deterministic herbivore-biased FNV hash, so the bowl stays predator-sparse) and tags every food `plant`/`meat` (`Food.kind`); `eats(diet,kind)` is a read only, deliberately **not** wired into feeding (herbivores still eat a meat hatch drop exactly as before). On that read, **Twitch** — the jittery compsognathus, the roster's lone theropod (the backlog guessed "Glade", a parasaurolophus; diet is seeded by species, not the placeholder name) — becomes the bowl's only hunter. When Twitch's pressing need is hunger, a new pure `world/foodweb.ts` (`nearestPrey` within `STALK_RANGE=6` Chebyshev, `fleeStep` away-with-wall-slide, `huntCaught` ≤1) drives a stalk: `forceStep` pairs each hungry, in-view carnivore off cooldown with the nearest in-view herbivore, then the hunter closes (🎯 `stalking`) and the quarry bolts (💨 `fleeing`) — the stalk slots just below the real-food-rush (a sure meal still wins) and above social/wander. **Deathless by construction:** when Twitch reaches the prey the hunt *comes up empty* — the quarry escapes (💨 + "🦖 the hunt came up empty — <prey> slipped away" event), Twitch rests on a 30s `HUNT_COOLDOWN_MS`, and each side files a memory ("your hunt for X came up empty" / "you slipped X's hunt"). No catch-kill, no hunger relief from the chase (meat-from-a-take is 437), no roster mutation — mortality stays a CHARTER call routed to the operator. No save field (diet derived from species; the memory is the durable trace). +14 unit tests (diet ×6, foodweb ×8). **Milestone 3 lore arc 2 ✅.** (lore track)
- BACKLOG-433: Each zone's harvest reads on its own — the farming signal steps out from behind the tier. BACKLOG-428 already split the harvest count per zone (`harvestedByZone`) and folded it into each zone's prosperity badge, but a zone's farming output was only ever visible *blended* into the ○/◐/● tier. Now the map lens (425) reads it plainly: `ZoneMapEntry` gains `harvested`, `zoneMapModel` takes a trailing `harvests` map (default `{}`, so every cycle-96 3/4-arg caller stays byte-valid), and `drawZoneMap` renders `🌾N` on the badge line beside the tier. Harvest the bowl's plot → its box shows `🌾1` while the grove and Fernreach hold `🌾0`; each zone's farming reads on its own, not as one bowl-wide number. Rides the existing additive `harvestedByZone` save field — no schema change, no tier regression. +2 unit tests. **Milestone 3 structure arc 3 ✅ — milestone complete.** (structure track)
- BACKLOG-439 [infra] (seeded): stand up a real e2e harness — this cycle surfaced that the repo tracks **no** test files (the studio had been narrating a vitest/playwright suite that was never committed). Cycle 99 landed the first 16 real unit tests; 439 tracks adding `playwright.config.ts` + a boot spec so the WorldScene glue can be *executed*, not only reviewed.

## Cycle 098 — 2026-07-11 (Milestone 3 "Enough to go around" — 4 of 6 arcs)
- BACKLOG-385 + BACKLOG-386: Provision remembered — the generous feeder (375) closes its loop. Since cycle 83 a well-fed dino has stepped back and let a hungrier high-bond friend eat first, but the gift vanished the instant it was given. Now the fed friend throws a 💛 at its benefactor as it eats (386 — `lastNuzzle`/`__nuzzle`, a `💛 <friend> nuzzled <giver> in thanks` line) and *files who fed it* (385). Later, deciding its own yield, it repays that benefactor at a relaxed bar it would cross for no one else: `yieldFoodTo` gains an optional `owes` set, and an owed benefactor qualifies at `RECIPROCAL_BOND=20` / `RECIPROCAL_HUNGRIER_BY=0.1` (vs the stranger-friend `GENEROUS_BOND=40` / `HUNGRIER_BY=0.25`) and wins ties (owed-first sort) — so a dino repays a friend at bond 25 that a mere acquaintance would never get. The repayment clears the debt (a one-shot ledger, so kindness keeps cycling) and files a "you repaid X's kindness at the hatch" memory. Empty ledger ⇒ byte-identical to the cycle-83 verdict (regression-pinned). `owesFood` is a live per-session ledger — no save field; the durable trace is the persisted memory, exactly as 375 kept none. No WebLLM, gobble/stand-up paths untouched. +5 unit / 2 e2e. **Milestone 3 lore arc 3 ✅.** (lore track)
- BACKLOG-432: Fernreach plot + a farmable third crop — all three zones farm now. The Fernreach — the third zone since cycle 85 — grew nothing (349 cut only bowl + grove plots) and FOODS held just two plant crops, both taken by 418. Adds a third farmable food, `roots` (🥕 "starchy roots", appeal `{energy:-0.5, bravery:0.4}` tuned so it flips no roster dino's favorite in any season — 061/170/418 verdicts pinned intact), the Fernreach's crop identity (`CROP_BY_ZONE['fernreach'] = {food:'roots', ripe:'🍠'}` — the 🍠 marker distinct from the sprout 🌿, the roots food's own 🥕, and the bowl/grove 🍓/🥬), and a Fernreach plot tile (`FERNREACH_PLOT_TILE {8,8}`, Fernreach grass clear of the west creek, fern bands, and edges). The plot machinery is already fully zone-generic (it iterates `PLOT_TILE_BY_ZONE`), so plant/ripen/harvest/per-zone-tally light up for the Fernreach with only a `fernreachPlot` save field (additive, `readPlot`-validated, old saves → null) + its restore key. The three-zone farming divergence 418 started now reads complete. Save additive, no WebLLM. +7 unit / 2 e2e. **Milestone 3 structure arc 2 ✅.** (structure track)

## Cycle 097 — 2026-07-10 (Milestone 3 "Enough to go around" OPENED)
- BACKLOG-368: Hunger in the voice — the need-drive stops being just an icon and becomes something a dino *says*. A dino whose pressing need is hunger (`needs.pressingNeed(...) === 'hunger'`, 371 over `NEED_THRESHOLD`) lets it slip into its greeting — a short, temperament-shaded aside appended to whatever register it was already in. New pure `hungryAside(traits)` in `ai/brain.ts`: prickly (`agreeableness < PRICKLY_MAX`) grumbles "…and I'm starving, if the keeper's asking.", warm (`> EFFUSIVE_MIN`) pleads "…I'm *so* hungry… could you spare a bite?", even-tempered just mentions "…could eat, honestly." `cannedReply` computes the base line (gratitude/wistful/fond/generic) then appends the aside when `ctx.hungry`, so the tell **composes with every register** rather than fighting the ladder — hunger leaks regardless of topic. `webllmBrain` gains a hunger clause in the system prompt (LLM *colour* only; the canned aside is the deterministic floor, so behavior never depends on the model). `WorldScene` greet ctx sets `hungry = pressingNeed === 'hunger'` — hunger-specific, so a thirsty-but-not-hungry dino stays quiet. No save change, WebLLM stays under `ai/`. +7 unit / 3 e2e. **Milestone 3 lore arc 1 ✅.** (lore track)
- BACKLOG-429: Zone carry pressure — the three-zone larders start talking to each other. A per-zone stockpile *soft* cap (`STOCKPILE_SOFT_CAP=6`, below the per-kind hard cap so it bites first): when a crossing dino leaves a zone over the soft cap **toward a strictly-lighter neighbour**, the carry link (329/356/377) sheds the glut — up to `PRESSURE_CARRY=2` of its most-stocked acceptable kinds (`pickCarry`), so banked resources flow toward need instead of piling forever in one zone. New pure `world/resource.ts`: `pileTotal`, `overSoftCap`, `pressuredCarry(src,dest,recipe)` — not over cap / dest not lighter → the single directed kind, byte-identical to today (`[directedCarry(...)]` or `[]`); under pressure it simulates each transfer on working copies so every unit re-checks the dest cap (lossless + cap-safe). `WorldScene.crossDino` applies the returned list; resources are never pushed into an already-fuller zone. Additive save (piles already persist; no schema change). +7 unit / 2 e2e. **Milestone 3 structure arc 1 ✅.** (structure track)

## Cycle 096 — 2026-07-09 (Milestone 2 "Places to belong" SHIPPED 🎉)
- BACKLOG-428: Zone prosperity index — **Milestone 2's closing arc.** Each zone's four live signals — banked resources (328), built landmarks (286/315/417), resident heads (316), and crops harvested from its plot — fold through a new pure `world/prosperity.ts` into one score + a three-step tier, shown as a badge on the map lens (425). `zoneProsperity({stockpile,structures,heads,harvested})` = `structures*3 + heads*2 + harvested + stockpile` (non-negative, monotonic — the rarer signal weighs more); `prosperityTier` cuts at ≤3 `quiet ○` / ≤9 `growing ◐` / else `thriving ●`; `prosperityBadge`. `ui/lenses.ts`: `ZoneMapEntry.tier` + a 4th `tiers` arg on `zoneMapModel` (defaulted → 3-arg callers unchanged). `WorldScene`: `zoneSignals(id)`/`zoneTiers()`, the tier fed into `zoneMapEntries`, a third map-box line (boxH 52→64), `__zoneProsperity` hook. To give the farming term an honest per-zone value (the global `harvested` isn't split yet), a minimal `harvestedByZone` counter is bumped at the single harvest site and round-tripped additively through the save (old save → `{}`, malformed → reject) — lightly overlapping BACKLOG-433, whose residual scope becomes *surfacing* per-zone farming as its own line. "Which zone is thriving" is now a glance; the foundation the deferred governance/festival items (031/026) read instead of re-deriving. No WebLLM, save additive. +8 unit / 2 e2e. **Milestone 2 structure arc 3 (final) ✅ — milestone ships.** (structure track)
- BACKLOG-410: Homesick sooner — the belonging arc turns its mirror to what a *strange* zone feels like. A dino not yet settled (341) with no bonded friend (≥ the 8-pt grief floor) residing in its current zone falls into its signature tic (405) at `TIC_AFTER_STEPS_HOMESICK=12` instead of the plain 20 — "isolation in an unfamiliar place reads quicker." Pure `world/tic.ts` `aloneInStrangeZone(settled, hasFriendInZone)` + a `Math.min` with the 393 solitary-day threshold (the two shorteners compose); `WorldScene` `zoneMates(d)` feeds the existing `closestFriend`, and `__tic` reports a live `strange` flag. **Scope cut:** the planned `strangeZoneTicMemory` was dropped — 410's gate also matches the plain-405 lone dino, so a distinct memory would have *replaced* the shipped "a little ritual of your own" memory (a 405 regression), and it could never coexist with the 414 grief memory anyway (unreachable). 410 ships as the onset shortening only — exactly the item's ask ("falls into its tic *faster*"); the ritual + its memory are unchanged, 405/414 intact. Off-milestone (the milestone's lore checklist was already complete) but in the belonging theme. No WebLLM, no save change. +3 unit / 2 e2e. (lore track)

## Cycle 095 — 2026-07-08
- BACKLOG-340: Homesick for a friend — the grief tic gains legs. Cycle 93 gave a dino a home it settles into (341); cycle 94 gave it a direction to face when a close friend leaves (414); now that ache moves the dino. A resident whose *closest* bonded friend (013, above the same 8-pt grief floor) lives a zone away gets **homesick**: after residing `HOMESICK_ROLLS=2` migration rolls in the friendless zone, it sets off back across the chain toward that friend — one zone per hop via the 414 `griefEdge` direction — files a one-time memory that names them ("you miss Twitch — the zone feels lonely without them, so you drift back to find them"), and floats a 🧭 beat. The homesick pull is a *preferred* migrant pick AND **overrides the 341 settle-resist**, so a settled-but-lonely dino still leaves — "company, not scenery, decides where a dino settles" is enforced in the migration math, and two mutual friends split by the ambient wander quietly reunite. New pure `world/homesick.ts`: `homesickDest(name,myZone,bonds,others,zoneOf,rolls)` (reuses `closestFriend`+`GRIEF_BOND_FLOOR`+`griefEdge`+`neighborThrough` so 340/414 always agree) + `homesickMemory` + `HOMESICK_ROLLS`. `WorldScene`: `homesickOf`/`tryHomesick` (shared by `maybeMigrate` and the `__homesickMigrate` hook), `pickMigrant` homesick-preferred, the homesick branch checked before the settle-resist gate. No WebLLM, no NPCBrain breach, no save change. +7 unit / 3 e2e. **Milestone 2 lore arc 3 (final) ✅.** (lore track)
- BACKLOG-418: Per-zone crop identity — the farming half of the economy diverges per zone. The plantable plot grew the same 🍓 berry in every zone even though gathering already diverges under the resource bias (348) and the chain raises three distinct skylines (417). Now the crop is a per-zone value: new `plot.ts` `CROP_BY_ZONE`/`cropOf`/`stageGlyph`/`ZoneCrop`. The **bowl** keeps its **berries** — byte-identical: same food dropped, same 🍓 marker, same baked berry-bush pixel prop. The shaded **grove** grows **leafy greens** with a distinct 🥬 ripe marker (chosen so the plot never reads as its own 🌿 sprout stage *or* the greens food's 🌿), releasing greens (not berries) into the feeding loop when harvested. `drawPlotSprite` bakes the shared soil-mound rig for seed/sprout and the berry-bush only for a *berry* ripe plot; a non-berry ripe plot falls back to its crop glyph until an [art] fire draws its rig. `harvest`/ripen logs read the zone crop marker; new `__plotGlyph` hook. Additive — grove plot already persisted, no version bump; `CROP_FOOD_ID` kept as the bowl default. One pre-existing spec (cycle-079) that pinned the old grove=berries value updated to greens (the intended change, not a regression). **Honest scope:** the Fernreach's own plot is deferred (no plot today + FOODS has only two plant crops) and seeded as BACKLOG-432 (a farmable third crop first); two-zone divergence already delivers the arc. +6 unit / 2 e2e. **Milestone 2 structure arc 2.** (structure track)

## Cycle 094 — 2026-07-07
- BACKLOG-414: A ritual for the missing friend — the solitary tic gains a *direction*. Milestone 1 gave a dino a self; cycle 93 gave it a home it settles into (341); now a departure leaves an ache you can see. When a dino falls into its private ritual (405) and its *closest* bonded friend (013, above an 8-pt floor) lives in another zone, it walks its tic to the linked edge that leads toward that friend and performs it there — the same little pace/circle/fuss, now aimed at the boundary they crossed by — filing a one-time memory that names them ("your closest friend Twitch crossed away — you pace a fixed little path at the edge they left by"). New pure helpers: `world/tic.ts` `griefEdge(dinoZone,friendZone)` (the chain direction toward the friend, `zoneChain`-read so a fourth zone needs no edit), `griefAnchor(edge,row,cols)` (west→col 0, east→last col), `griefTicMemory`, `GRIEF_BOND_FLOOR=8`; `social/bonds.ts` `closestFriend(name,bonds,others,floor)` (the shared 013 pick, extracted from comfort.ts's inline copy). `WorldScene`: a transient `ticGrief` map, a `griefFor(d)` read, the tic block sets the grief anchor on the first ticcing step and *walks* to it via `stepToward` (non-grief anchor === current tile, so byte-identical), `performTic` files the directional memory + log, `resetTic` clears the grief, `__griefTic` hook. No WebLLM, no save change, deterministic floor intact. +9 unit / 2 e2e (+ cycle-087 hardened for determinism). **Milestone 2 lore arc 2.** (lore track)
- BACKLOG-431: Pause ambient timers in e2e — the parallel-load flake class killed at its root. 300+ specs run full-parallel against one dev server, and the sim's wall-clock background timers (wander `forceStep` @3s, sky roll @45s, migration roll @90s) kept mutating world state between a spec's setup and its assert, reddening a random spec each CI run ("Expected 1 Received 0"). A scene flag `ambientPaused` (default `false` — normal play unchanged) gates those three timer *callbacks*; the shared e2e `boot()` in `tests/e2e/helpers.ts` sets it, so every spec gets a still world for free, while the explicit dev hooks (`__stepWorld`, `__triggerSky`, `__migrate`, `__maybeBarter`, `__advanceWall`) call the underlying methods directly and bypass the gate. New `__pauseAmbient`/`__resumeAmbient`/`__ambientPaused` hooks. The full 308-spec suite is green under parallel load — and the previously-red `mobile-minds` long-dialog spec (catalogued BACKLOG-430) now passes too: the ambient chatter racing it is frozen. Test-only surface, additive, no NPCBrain/save impact. Operator-injected, shipped ahead of 418. +3 e2e. (structure track)

## Cycle 093 — 2026-07-06 (Milestone 2 "Places to belong" opens)
- BACKLOG-417: Fernreach frond thatch — the chain gets its third skyline. Zone-distinct craft (377) mapped a zone's resource bias to the landmark it raises — stone-rich bowl → 🗿 cairn, branch-rich grove → 🛖 lean-to — but the third zone leans a **frond** (400) and 377 left `frond → 'cairn'` as an explicit placeholder, so three zones raised only two kinds of landmark, and the frond-rich Fernreach scraped cairns off the 25% off-kind it barely gathers. 417 gives the frond bias its own structure: a woven frond **thatch** 🥻 built from `{frond:4}` — the kind the Fernreach actually banks — wiring in the pixel rig stashed last cycle (BACKLOG-427). `resource.ts`: `Structure += 'thatch'`, `THATCH_RECIPE`/`THATCH_GLYPH`, `STRUCTURE_BY_BIAS.frond='thatch'`, a `structureRecipe` thatch arm, and a new generic `buildStructureFor(pile,zone)` that spends whatever `structureRecipe(zone)` returns so cairn/lean-to/thatch share one afford/spend path (the per-structure helpers stay for their tests + the barter fallback). `WorldScene`: `thatches`/`thatchSprites`, a three-way build dispatch, `drawThatch`/`placeThatch` off the baked `'thatch'` rig, zone-visibility toggle, additive save/restore, `__thatches`/`__thatchIsArt` hooks. The frond recipe now feeds directed carry / edge barter, but frond is Fernreach-exclusive (400) so those fall back to a spare with no cross-zone regression. Additive `thatches` save field, no version bump. +6 unit (incl. the cycle-088 placeholder contract updated frond→thatch) / 2 e2e. **Milestone 2 structure arc 1.** (structure track)
- BACKLOG-341: Home-zone settling — a dino starts to *belong* to a zone. A resident wandered between zones on a blind coin-flip (274/333); now a dino that stays put long enough **settles**, and a settled dino resists the ambient wander that would otherwise drift it away. Reshaped from the grove-specific "grove favorite" text to the general spine. New pure `world/belonging.ts`: a `Tenure` map counted on the migration cadence (`bumpTenure`/`resetTenure`/`tenureOf`), `SETTLE_ROLLS=4`, `isSettled`, and a damped `resistsMigration(settled, rand)` (a settled dino stays put ~60% of ambient rolls, `SETTLED_MIGRATE_DAMP=0.6`) plus `settledLine`. `WorldScene`: a persisted `tenure` field, `bumpTenures()` at the top of `maybeMigrate`, the settled-resist gate on the picked migrant, `resetTenure` on both `crossDino` and the instant `relocate` (a fresh zone starts fresh), a `bookRows().home` = "at home in <zone>", and `__settleTick`/`__tenure`/`__settled` hooks. `lenses.ts`: `BookRow.home?` rendered under the plans line. Where a dino belongs is now a legible standing in the collection book, and home means something. Deterministic floor intact — no WebLLM, the damp is the only randomness and it's unit-pinned via an injected `rand`; additive `tenure` save field (old saves settle from scratch). +6 unit / 2 e2e. **Milestone 2 lore arc 1.** (lore track)
- BACKLOG-430 [infra, opened not shipped]: QA surfaced that `mobile-minds.spec.ts` "long dialogs page GBA-style" fails on a clean HEAD in isolation (reproduced via `git stash`), so it is a genuine pre-existing break in the keeper-picker/dialog ArrowLeft paging path, not the parallel-load flake the cycle-92 verdict logged — filed for a follow-up fix so the e2e run reads all-green again.

## Cycle 092 — 2026-07-05 (★ Milestone 1 "Minds of their own" SHIPPED)
- BACKLOG-012: NPC daily plan — the day gets a shape. Cycle 90 put one flat intent on a dino's day; now the day has an *arc*. New pure `ai/plan.ts`: `proceduralPlan(name,day,traits)` composes a `DayPlan` — one lean (`social`/`solitary`/`forage`/`restless`) for each of the four `dayPhase`s (dawn/day/dusk/night) — trait-weighted off a `pickKind()` factored byte-identically out of `intent.ts` (one weight table now feeds both the whole-day intent and the plan), seeded per (name,day,phase) so a dino forages at dawn, drifts to company by midday, and keeps to itself at night, all in character. `WorldScene.ensureIntent` now derives the active lean from the *current* phase and re-derives when the phase or day turns, so the same clamped step-loop nudges (socialize / tic / forage / wander) shift across the day without ever reordering priority. Deterministic and recomputed each in-game day from name+day+traits — **no save field**, so it stays disjoint from the save-envelope track and a phone pays nothing. The LLM `intend()` enrichment is preserved, coloring the active-phase note. Player-visible as a `plans: forage → social → solitary → rest` line in the collection book; `__plan` hook. +7 unit / 4 e2e. **Milestone 1 lore arc 3 (final) ✅.** (lore track)
- BACKLOG-426: Versioned save envelope — the persistence spine rooted at its origin. The migration *rail* already shipped early (BACKLOG-040: `SAVE_VERSION`, an ordered `MIGRATIONS` table, the `migrate()` chain, a worked v1→v2 step); what 426's own text named but 040 never shipped was the v0 root — a *versionless* save was being **rejected** by `migrate()` and silently becoming a new game, a quiet data-loss trap. Now `migrate()` reads a missing `version` as v0 (floor lowered `<1`→`<0`) and lifts it through a new `MIGRATIONS[0]` v0→v1 no-op stamp, then v1→v2, so every save ever written — including the oldest pre-versioning shape carrying a modern additive field like `personas` — rides the chain intact instead of being discarded. A present-but-newer, non-integer, negative, or `null` version is still rejected; the chain stays pure (never mutates its input). One file (`saveGame.ts`); three older tests that pinned the now-overturned "versionless rejected" contract were updated to the rooted-at-v0 behaviour. +8 unit. **Milestone 1 structure arc 3 (final) ✅.** (structure track)

## Cycle 091 — 2026-07-04
- BACKLOG-103: Persona from lore — the CHARTER "Living minds" core goal, seeded 2026-06-01, finally ships: every dino has a **persona** — a backstory scrap, a want, a fear, a speech quirk — replacing the shared one-line roster adjective in every prompt. Where a model runs, `NPCBrain.author()` (optional; WebLLM implements via `buildPersonaMessages` from a new `PARK_LORE` canon blurb, stub omits, status-gated so it never triggers a download) writes the self ONCE — the pure `upgradePersona` guard makes an `'llm'` persona permanent, never re-authored. Everywhere else (headless CI, declined download, stub) a deterministic name-seeded `proceduralPersona` composes the self from authored want/fear/quirk/backstory tables, trait-shaded, roster flavor kept verbatim — same name, same self, every device, byte-identical. Generated at first need, cached (`ensurePersona`, the `ensureIntent` shape, governor-gated), persisted in an additive `personas` save field (undefined-when-absent, no version bump, old saves regenerate deterministically), restored byte-identical, and fed as `ctx.personality` into all three brain call sites (greet / npc_meet / intent authoring) — `buildMessages` carried it with zero changes of its own. +11 unit / 3 e2e. Milestone 1 lore arc 2 ✅; the daily plan (104/012) now has a self to plan from. (lore track)
- BACKLOG-425: Zone map lens — the V ring gains a sixth page: the whole zone chain as one picture. Pure `zoneChain()` walks the 383 adjacency table west→east (any unlinked zone appends rather than vanishing) and `zoneMapModel` joins it to the 316 head counts + the keeper's zone; the scene draws labelled boxes (name + `n 🦕`), connectors along the links, and a keeper dot, live on every lens refresh. A fourth zone joins the map by adding its ZONE_LINKS row — zero UI edits, the same free-extension property the edge labels (398) proved. `'map'` appended at the END of the ring so every existing lens kept its position. +6 unit / 3 e2e. Milestone 1 structure arc 2 ✅; the save envelope (426) is the natural next pick. (structure track)

## Cycle 090 — 2026-07-03 (CHARTER v6 · Milestone 1 opens)
- BACKLOG-393: Brain-biased intent — the operator's cycle-85 nudge, and the first time the dino brain shapes *decisions* instead of speech. Each in-game day every dino gets an **intent** — social / solitary / forage / restless, a closed set — with a short in-voice note, and the day's lean scales the step loop's existing rolls (socialize chance 0.45→0.65/0.25, tic onset halved on a solitary day, resource notice widened on a forage day, "stay" re-rolled once on a restless day), all pure and clamped in the new `ai/intent.ts`, never reordering the huddle>gather>mope>tic>socialize>wander priority. Where a model runs, `NPCBrain.intend()` (optional; WebLLM implements, stub omits) authors the intent once per dino-day — async, cached, governor-gated, strictly parsed against the closed set — and where it doesn't, a seeded procedural author (name+day+traits through the personality rng) produces it deterministically, so headless CI, a declined download, and the stub run the FULL sim; the model only recolours a lean the floor already chose. Player-visible in the collection book ("today: …"). Best proof it's real: QA caught a forage-day dino gathering an organic grove branch mid-carry-spec — the mind moving the economy. Intents are transient (a reload re-authors); no save change. +17 unit / 3 e2e. Milestone 1 lore arc 1 ✅; 103 (persona) and 104/012 (daily plan) build on this spine. (lore track)
- BACKLOG-398: Edge indicator — the three-zone chain is legible before you cross. Every linked edge of the current zone shows a small label naming the neighbour ("The Grove ▸" east of the bowl; "◂ Pocket Cretaceous" + "The Fernreach ▸" in the grove; "◂ The Grove" in the Fernreach), read from the 383 adjacency table through one pure `edgeIndicators()` — a fourth zone will label itself by adding its ZONE_LINKS row, zero UI edits. Rendered as chrome (depth 7, edge-centered) by a `drawEdgeLabels()` hooked into `drawFloor()`, so boot, `__setZone`, and a real keeper crossing all relabel through the one call site. Crossing logic byte-identical (zone specs pin it). The operator's Idea-Box nudge closed; the map lens (425) reads the same table next. Milestone 1 structure arc 1 ✅. +4 unit / 3 e2e. (structure track)

## Cycle 089 — 2026-07-03
- BACKLOG-413: Fond of being caught — the catch (408) stops being uniform. Last cycle every dino caught mid-ritual went bashful the same way — a sameness bug in a game whose whole premise is that a tic makes a dino unmistakably itself. Now the reaction forks on *your* bond with that particular dino: below the close-friend floor (`FOND_MIN` hearts, the same threshold the fond greeting 272 uses) it's still sheepish (😳 + `bashfulOpener()` + the "went a little bashful" memory); at or above it the dino is *pleased* to be seen (😊 + a warm `fondOpener()` that shows the ritual off + a "you were glad it was them" memory). Pure `fondOfBeingCaught`/`fondOpener`/`fondCaughtMemory` in tic.ts (twins of the 408 bashful helpers, threshold imported from the `ai/brain` interface) + a two-line fork in `openToneMenu` (the glyph) and `pickTone` (the opener + memory), both computing fondness from the same `heartsFromPoints(friendship)` source so they never disagree. The fond frame wraps whatever the brain/stub returned, so the model is never asked to be fond and it tests headless (`NPCBrain` boundary intact). A non-fond catch and a not-mid-tic greet are byte-identical; no affinity delta, no bond, no save change. +3 unit / 1 e2e. Follow-ups 420 (caught again) / 422 (warmed by the catch) / 423 (tic-flavored voice). (lore track)
- BACKLOG-384: Resource regrowth — the gather economy stops being bottomless. Since 146 a zone re-rolled its resource slot forever at a flat chance no matter how hard it was worked (314 made it per-zone, still infinite). Now each zone carries a *yield* (fertility 0..1, starting full) that a pickup thins (`depleteYield`, ~3 gathers empty it) and each spawn-roll tick slowly restores (`regrowYield`, capped), with the spawn chance scaled by it (`yieldSpawnChance = RESOURCE_SPAWN_CHANCE × yield`) — so over-gathering a zone stalls its spawns until it rests, and an exhausted zone goes quiet until it regrows. The first renewable constraint, and the first real economic reason to carry/barter (329/356/358) from a fresh zone into a worked-out one. Pure `world/regrowth.ts` (deplete/regrow/scale/roll) + a `yieldByZone` field, a regrow+scaled-roll in `maybeSpawnResource`, a deplete in `checkGather`, and a `__yield` hook. A full zone spawns at exactly the old rate, so an unworked zone is pre-384 byte-identical (the cycle-062/078/088 resource specs pin it). Yield is transient — a reload restarts each zone fresh-full — so no save-format change. The e2e proves depletion through the live gather path, not just the pure math. +5 unit / 1 e2e. Runners-up 398/417/418 queued. (structure track)

## Cycle 088 — 2026-07-02
- BACKLOG-408: Caught mid-tic — the solitary tic (405) meets the social world. Greet a dino deep in its private ritual and it startles: a 😳 the instant the greet opens, and its reply comes out bashful — a deterministic `bashfulOpener()` prefixed to whatever the brain (or the stub) returned, so a caught dino sounds sheepish without the model ever being asked to be (the `NPCBrain` boundary is untouched, and it tests headless). It files a one-time "the keeper caught you mid-ritual — you <label>, and went a little bashful" memory naming the ritual. Because the player isn't a dino, walking up never counts as the company that would break the tic (405's `companyNear`), so a dino can genuinely be caught. Pure `bashfulOpener`/`caughtMemory` in tic.ts + greet-path glue in WorldScene (`openToneMenu` snapshots `caughtTic` + flashes 😳; `pickTone` prefixes + files-once, now returns the shown line; `closeToneMenu`/`resetTic` clear the guards so the frame can't leak). A non-mid-tic greet is byte-identical; no tone-delta, no bond, no save change. +3 unit / 1 e2e. Follow-ups 412 (self-soothing) / 413 (fond of being caught). (lore track)
- BACKLOG-400: Third-zone resource bias — the three-zone chain finally gathers three different things. Resource bias (348) was a two-row table (bowl→stone, grove→branch) so the Fernreach inherited the uniform 50/50 fallback; now it leans its own third kind, a 🌾 frond. Because the off-kind pick is always a *primary* (branch/stone), frond is Fernreach-exclusive and the bowl/grove distributions are byte-identical (the cycle-078 parity, pinned by frond-exclusion assertions). The work was almost purely `resource.ts` — a new `ResourceKind`, a glyph, the bias row, and completing `STRUCTURE_BY_BIAS` (frond→cairn until its own structure, 417); the `RESOURCE_GLYPH` kind registry and the un-whitelisted `stockpileByZone` validator meant the stores readout, carry, barter, sprite draw, and persistence all picked frond up with no WorldScene source change and no `SAVE_VERSION` bump. Frond isn't a craft kind yet, so `directedCarry` never pulls it for the cairn recipe (spare-only), and `PROP_RIGS` being string-keyed means the missing frond pixel rig cleanly falls back to the emoji glyph. +8 unit / 1 e2e. Frond-distinct structure = 417, per-zone crop identity = 418. (structure track)

## Cycle 087 — 2026-07-01
- BACKLOG-405: Solitary tic — after four cycles spent on the hatch standoff (a dino always reacting to another over a scrap), a dino finally does something that's *its own*. Left genuinely alone — no company in its zone within range, no pressing need, nothing to do — it accrues a solitary streak and, past `TIC_AFTER_STEPS`, falls into a small ritual keyed to its most-pronounced trait: it paces a fixed little path, fusses over one spot, or turns a slow circle (`signatureTic` over `TIC_BY_AXIS`), floating its glyph and filing a one-time "a little ritual of your own" memory. Pure `world/tic.ts`; `forceStep` gains a `ticcing` branch below `moping` and above pointless cross-zone `socializing`, with the solitude counter resetting only on real disturbance — so a lonely loner still withdraws to the edge first and forms its ritual on a calm step. Model-free, name-seeded, deterministic; no bond change, no save change (transient state + the additive memory ring). +9 unit / 1 e2e. Follow-ups 407–411. (lore track)
- BACKLOG-358: Edge-meet barter — the inter-zone economy goes two-way. Carry (329 → 356 → 377) only moves a resource in the direction a dino walks, on a crossing; now two dinos who *linger* at their zones' shared edge trade, each handing over the kind the other's zone is short of for its next structure. Pure `barterSwap` (`directedCarry` run both ways) in resource.ts + `nearLinkEdge` (over the 383 adjacency table) in zones.ts + a dwell-gated `maybeBarter`/`doBarter` pair in the `forceStep` tail. Conserved and cap-safe on the same lossless `takeResource`→`bankResource` path carry uses; no bond change (economic beat only). The one hazard — the ambient scan mistaking an *arriving crosser* for a meet and barter-ing back a just-carried resource — was caught in-fire and fixed by gating the scan to dinos parked at the literal edge column (`band=0`, `EDGE_DWELL=2`), restoring carry conservation. +6 unit / 2 e2e. (structure track)

## Cycle 086 — 2026-06-30
- BACKLOG-394: Backed-down gobbler slinks off — the contested-drop standoff now reads from *both* sides. Cycle 85 (390) gave the bold winner its moment (it holds its tile, the gobbler "backs down"), but the gobbler's side was silent. This cycle the denied gobbler also reacts: a 😖 flash + a "<bold> wouldn't budge — you slunk off" memory + a log line, so the failed grab carries a visible cost. Pure `slunkOffMemory(bold)` in feeding.ts consumed in `checkFeeding`'s existing stand branch — the bold winner still eats; the timid-winner gobble (387) + the 375 yield are byte-identical (the slink fires *only* on a bold stand). No bond change (395 owns the social ripple), no save change (the memory rides the additive ring). +2 unit / 2 e2e. Follow-ups 395-397. (lore track)
- BACKLOG-399: Third-zone terrain identity — The Fernreach stops being tinted grass and becomes a *place*. 378 opened the third zone but `drawFloor` baked a real terrain map only for the grove; everything else fell through to plain grass. This cycle generalizes that hard-coded `inGrove` branch into a data-driven `zoneTileAt(zoneId,...)` dispatcher and gives the Fernreach its own `fernreachTileAt` layout — a water creek down the west side (reusing the already-drawn `WATER_RIG`, so a real feature shows now) and bands of a new `fern` scrub kind, laid out unlike the grove's central trail + NE pond. The `fern` kind grass-falls-back under the warm `FERNREACH_TINT` until the Artist draws its rig (the 294→033 discipline: layout spine first, art follows) so the floor is always whole. Ends the Artist's long no-op streak — the fern scrub is the first renderable terrain art in many cycles. Bowl + grove floor keys byte-identical; no save change (terrain computed from the zone id). +9 unit / 1 e2e. Edge indicator 398 / resource bias 400 queued. (structure track)

## Cycle 085 — 2026-06-29
- BACKLOG-390: Standing up to the gobbler — the contested-drop trio is complete. Two cycles built who *gives way* at the hatch (the generous yield 375, the greedy gobble 387); this one adds the dino who *won't*. When a hungry, prickly dino would shoulder past a winner that's keeping its food (387), a **bold** winner instead holds its tile — the gobbler backs down, the winner eats, 😠, "stood your ground and kept your food from <gobbler>". Pure `standsGround(winnerBravery)` + `STAND_BRAVERY=0.65` in feeding.ts, consumed in `checkFeeding`'s no-yield branch between gobbler-detection and the gobble; so the same scrap now reads three ways by trait — generous / greedy / unbowed — with bravery (the startle + first-contact axis) deciding who gets pushed around at dinner. Kept honest: the yield (375) still pre-empts (it returns first) and a *timid* winner is gobbled byte-identical to cycle 84 (its spec's winner pinned timid in-fire, the same 1-line isolation 84 used for 375). No bond change (062/394's job), no save change. +2 unit / 2 e2e. Follow-ups 394–397. (lore track)
- BACKLOG-378: Third zone spine — the cycle-84 adjacency graph (383) promised the third zone would "slot in as a table row"; this cycle cashes it. **The Fernreach** sits east of the grove — the first zone reachable only *through* another — added as a registry entry + two `ZONE_LINKS` rows. The keeper crossing generalized for free (already edge-keyed through the table). The real work was migration, whose walk/cross/entry helpers derived a single edge from `linkEdge(home)` and so would only ever send a grove dino *west to the bowl*: generalized by an **optional `edge` param** (defaulting to the old single edge, so every existing call site and the cycle-073/084 migration-column assertions stay byte-identical) plus a small transient `migrationCross` record that fixes each migrant's destination + edge for the whole walk. A grove dino now migrates east into the Fernreach (proven deterministically); the ambient roll spreads dinos across the whole chain; occupancy, the 3-zone tally, the both-zone stores readout, the floor tint (`zoneTint`/`FERNREACH_TINT`), and the per-zone economy (lazy `pileFor`) all generalize. The bowl↔grove *behavior* is byte-identical — three specs had stale **world-shape** facts truth-updated (the table is 4 rows; grove-east is no longer unlinked; the tally lists three zones). No save change (`dinoZones` already persisted any zone id additively). +9 unit / 3 e2e. Next: edge indicator 398, Fernreach terrain 399, resource bias 400. (structure track)

## Cycle 084 — 2026-06-28
- BACKLOG-387: Greedy gobble — generosity has an opposite, and now you can read it. Last cycle a well-fed dino learned to *give way* at the hatch (375); this cycle the same drop reveals the other pole — a hungry, prickly dino won't wait its turn and shoulders past a friend to a contested meal (😤), so yielding vs grabbing reads as a *trait*, not a universal rule. Pure `gobblesFood(hunger, agreeableness)` + `gobblerAmong(winner, winnerHunger, candidates)` in feeding.ts (the deterministic inverse of `yieldFoodTo`: a candidate qualifies when it's hungry ≥ `GOBBLE_HUNGER=0.5`, prickly ≤ `GREEDY_AGREE=0.35`, AND at least `HUNGRIER_BY=0.25` hungrier than the winner; hungriest first, ties toward the pricklier). `checkFeeding` runs it only in the no-yield branch — so a generous yield (375) still pre-empts — and on a gobble the shoulder-past dino eats via `eatFood` while filing a "shouldered past <winner>" memory and flashing 😤; otherwise the winner eats, byte-identical to before. No bond change (that funk/standoff is 390/062's job), no save change. +9 unit / 2 e2e; the cycle-083 generous passthrough test was updated one line (warm the stranger) to isolate 375 from the new gobble. Follow-ups 389–392. (lore track)
- BACKLOG-383: Zone adjacency graph — the spine refactor before the third zone lands. The bowl↔grove link was hard-coded as a binary in five `zones.ts` helpers (`linkedZone`/`otherZone` + the three migration helpers, all encoding bowl-east↔grove-west), so a third zone (378) or edge-meet barter (358) would mean editing every one. Now a single data-driven `ZONE_LINKS: {from, edge, to}[]` table — read by two small helpers `neighborThrough(zoneId, edge)` and `linkEdge(zoneId)` — is the source of truth, and every helper reads it: `linkedZone` keys its entry-x on the *exit edge* (not the zone id), `otherZone` looks up the neighbour (keeping the old unknown-id→grove default), and the migration trio branch on `linkEdge`. Behaviour is byte-identical for the only pair that exists: every signature is unchanged, WorldScene needed no edit, and the cycle-059 + cycle-073 zone/migration suites pass unmodified as the guardrail (plus a new cycle-084-zone-adjacency parity test). A pure, behaviour-preserving refactor — the third zone now slots in as a table row, not a rewrite. No save change. (structure track)

## Cycle 083 — 2026-06-27
- BACKLOG-375: Generous feeder — the need-drive (371) reaches between dinos and becomes kindness. A well-fed dino that wins the rush to the keeper's drop, standing beside a hungrier high-bond friend, now steps back and lets the friend eat first — the first generosity that costs the giver something (the friendship gain it forgoes). Pure `yieldFoodTo(winner, winnerHunger, candidates)` in feeding.ts (null when the winner is hungry over `WELL_FED=0.3` or no candidate clears both bars — bond ≥ `GENEROUS_BOND=40` AND ≥ `HUNGRIER_BY=0.25` hungrier — else the hungriest qualifying friend, tie → higher bond, deterministic); `checkFeeding` builds the swarm (in-view dinos within `SWARM_RADIUS=4` of the food) and, on a yield, feeds the friend via `eatFood` while the winner files a "let <friend> eat first" memory, gains +5 bond, and flashes 🤝. The no-qualifier path and the favorite/comfort/cold-warm paths through `eatFood` are byte-identical (they just run on the friend). No save change — the generous memory is the record, the beat is transient. +7 unit / 2 e2e. Follow-ups 385–388. (lore track)
- BACKLOG-377: Zone-distinct craft — the build arc's divergence finally shows in the *landscape*, not just the piles. Both zones used to run the same cairn→lean-to escalation (286/315); now each builds the structure its resource bias (348) favors — the stone-rich bowl stacks 🗿 cairns, the branch-rich grove raises 🛖 lean-tos — so the two skylines read apart. Pure `zoneStructure`/`structureRecipe` + `STRUCTURE_BY_BIAS` in resource.ts; `checkGather` swaps the `SHELTER_AFTER_CAIRNS` escalation for the bias choice; `crossDino` ferries toward the destination zone's *own* structure recipe via `directedCarry`'s recipe seam (356's first real second caller), so a grove short of stone for its lean-to pulls stone and a bowl short of branch for its cairn pulls branch. The cairn/shelter pile-math is unchanged — only the selection moved; the cycle-074 shelter spec was rewritten to the bias-divergence truth and the retired escalation constant breaks no saves. No save-format change. +6 unit / 2 e2e. (structure track)

## Cycle 082 — 2026-06-26
- BACKLOG-357: Both-zone stores readout — the divergence the build arc built is finally visible standing still. The plaque's Stores line showed only the keeper's *active* zone pile (328); now it shows both, `Stores · ▸Pocket Cretaceous 🪨 2 · The Grove 🪵 3`, ▸ on the active zone exactly the way the zone tally (316) marks population — so you watch the bowl and grove economies pull apart without crossing. Pure `zoneStoresLine` in plaque.ts (a twin of `zoneTallyLine` over `ZONES`, fed a per-zone glyph string so no plaque↔resource import) + a `zoneStores()` method at the two plaque sites. An empty zone drops out; both empty → no Stores line, byte-identical to the pre-357 case (`PlaqueStats.stockpile` stays a string, every `plaqueLines` back-compat test green). No save change. +4 unit / 1 e2e. Edge barter (358) + zone-distinct craft (377) build on the now-legible economy. (structure track)
- BACKLOG-374: Comfort food — solace, per palate. A moping loner that eats *its own* favorite food (061) now gets a quiet 😌 beat a plain meal never gives — a "comfort food — the silver fish eased the ache 😌" memory + a floated bubble — so who is soothed by what becomes a per-dino tell. Honest to 369's model: the 🥀 still lifts only when a real bond forms, so comfort food is a *momentary* per-palate solace, not a status flip (it mirrors the 135 💐 greet-perk). Pure `comfortsLoner`/`comfortFoodMemory`/`comfortFoodLine` in loner.ts (predicate unit-pinned across the favorite×loner truth table) + a six-line block in `eatFood` after the favorite path, so the plain favorite beat (gain 9 / 😋 / favorite memory) + hunger-sate (371) + cold-mend (184) stay byte-identical above it. No save change (loner status derives from the saved bonds). +5 unit / 3 e2e. Follow-ups 379–382. (lore track)

## Cycle 081 — 2026-06-26
- BACKLOG-356: Directed carry — the trade route learns to *balance*, not just move. Carry (329) ferried the source's most-stocked spare, blind to what the destination wanted; now a crossing dino brings the kind the destination zone is most short of for its next craft (the always-on cairn recipe `{branch:3, stone:2}`), so once the zones diverge (348) the route pulls both piles back toward buildable. The bowl swimming in stone but short a branch sends its branch to a grove that needs it, instead of shuffling stone for stone. Pure `directedCarry(src, dest, recipe?)` in resource.ts — the largest-deficit recipe kind the source can supply and the destination can still accept, falling back to `pickCarry` (a spare still moves) when the destination is fully stocked; one-line swap in `crossDino`. Same lossless transfer path (`takeResource`/`bankResource`, source −1 / dest +1), so the cycle-077 conservation spec stays green. No save change. +6 unit / 1 e2e. Both-zone readout (357), edge barter (358), and zone-distinct craft (377) build on the now-purposeful route. (structure track)
- BACKLOG-369: The loner finds a friend — loneliness becomes a state a dino *leaves*, not a label. Cycle 80 gave the friendless dino a 🥀 and an outsized greet (135) but left the door shut: nothing marked the moment it grew out of it. Now the first time a loner's bond clears the floor (`LONER_FLOOR=8`) — by meeting someone in the mill, or the keeper bonding it — it files a quiet "found a friend — not so alone now" and a 🌱 perks up over its head; the 🥀 lifts on its own (it always read the live bond graph). Fires once ever per dino. Pure `liftsLoner`/`foundFriendMemory`/`foundFriendLine` in loner.ts (a two-snapshot transition read over the bonds); a transient `lonerFriended` guard + a `checkLonerLift` wired at the meeting bond-bump and the `__bondPair` hook — no save change (the memory is the persistent record, so a reload can't re-fire). The payoff the cycle-80 validator named when it shipped the probabilistic mope-drift; the lonely-lean-on-keeper beat (370) is next. +5 unit / 2 e2e. (lore track)

## Cycle 080 — 2026-06-26
- BACKLOG-371: Need-drive spine — the bowl gets hunger and thirst. For twenty cycles a dino wanted nothing on its own clock; now each carries a hunger and a thirst that build over realtime at a trait-shaped rate (the eager burn through both faster), cross a threshold, and wear it — a 🍖 over the hungry, a 💧 over the parched — and resolve through systems that already existed: eating at the hatch sates hunger, reaching the grove pond sates thirst. Deliberately **deathless** (the operator's death question is a CHARTER call, routed upstairs): pure `world/needs.ts` (`advanceNeeds`/`pressingNeed`/`satisfy`) + a one-line `checkNeeds()` at the forceStep tail + a `satisfy(…, 'hunger')` at the eat site; the module never removes a dino (a need pinned at max over 5000 steps leaves the population unmoved). Additive `needs` save, no `SAVE_VERSION` bump. The structural answer to the operator's hunger/thirst nudge and the foundation for hunting (367) + hunger-in-voice (368); the wander-pull toward food/water is the deferred 372. +5 unit / 3 e2e (+3 saveGame). (structure track)
- BACKLOG-135: The loner — the bond graph finally speaks about the dino with no friends. A dino whose every pairwise bond sits below the floor (013) withdraws toward the bowl wall and wears a 🥀, and a keeper greet lands extra-hard on it — exactly `LONER_BONUS` over an identical non-loner greet, with a 💐 perk-up the moment you notice it. Pure `world/loner.ts` (`isLoner`/`edgeTarget`); the edge-drift is **probabilistic** (`MOPE_CHANCE`) so a fresh all-unbonded bowl doesn't deadlock every dino against a wall — a loner still mills enough to meet someone and grow out of it, so loneliness is a state to leave, not a sentence (the 🥀 lifting on a first bond is queued as 369). The `LONER_BONUS` lives in the plain branch of `recordGreet`/`recordTone`; no save change (status reads live off the saved bonds). Two greet-delta specs (035/032) isolated from the new bonus in-fire. +6 unit / 2 e2e. (lore track)

## Cycle 079 — 2026-06-25
- BACKLOG-349: Grove plot — the second zone learns to grow. The grove gathers (314), banks (328), leans its own resource mix (348), and trades over the carry route (329), but it grew nothing — the plantable plot (145) was a fixed bowl-only installation (308). Now the plot is per-zone: the single `this.plot` generalized to a `plotByZone` map, a new `GROVE_PLOT_TILE {4,10}` on grove grass (clear of the path band + NE pond), and plant/grow/harvest keyed by the active zone on the same realtime-day stage clock. Each plot draws and works only in its own zone (the grove plot is invisible from the bowl and vice-versa), so the grove is a genuinely self-sufficient economy — gather *and* grow. Pure `PLOT_TILE_BY_ZONE` in plot.ts (`PLOT_TILE` kept as the bowl alias so 145/317 are byte-identical, no-arg hooks default to the active zone); additive `grovePlot` save alongside the legacy `plot`, old saves load grove-empty, no version bump. +6 unit / 2 e2e (cycle-069 plot spec updated in-fire to the per-zone truth). Directed carry (356), both-zone readout (357), and barter (358) build on the now-complete second economy. (structure track)
- BACKLOG-359: First sight of the pond — the grove-news arc finally pays off at the water. Five cycles built the *pull* of the pond (arrival 339, news 342, the body it moves 345, common ground 346, the told-to-your-face sharpening 355); now a dino that crosses into the grove at its west edge and wanders across the clearing to the NE pond stops wide-eyed the first time it comes within sight of the water — a `💧 first saw the pond` memory + a `💧 …the pond…` bubble, once per dino ever. A *distinct* beat from 339 (which owns the first-grove-crossing look-around): it keys on pond proximity (`nearPond` scans the ±2 neighbourhood over the 294 terrain map for water) and its own `pondSeen` set, never `groveVisited`, so the two never collapse into one. Pure `nearPond`/`firstPondSight`/`pondSightMemory`/`pondSightLine` + `POND_SIGHT_RADIUS` in arrival.ts; `checkPondSight()` at the forceStep tail; `groveword.ts` untouched. Additive `pondSeen` save, no version bump. +8 unit / 2 e2e. Follow-ups: 363 reflection, 364 showed-them, 365 firsts-in-book, 366 pondside meeting. (lore track)

## Cycle 078 — 2026-06-25
- BACKLOG-348: Zone resource bias — the two zone economies finally gather *different* things. Each zone leans its resource roll toward its character — the grove's trees drop 🪵 branches, the bowl's open ground turns up 🪨 stones — instead of both rolling a uniform 50/50 (314). A *lean*, not a lock: `pickKind(rand, zone?)` returns the zone's favored kind at `BIAS_WEIGHT` 0.75 and the off-kind past it, so neither zone goes single-resource. Pure `ZONE_BIAS {bowl:stone, grove:branch}` + `BIAS_WEIGHT` in resource.ts (resource.ts now imports the zone ids — no cycle, zones.ts has no imports); `maybeSpawnResource` passes the spawning zone; an omitted/unknown zone keeps the old uniform path, so every 146/314/328/329 spec is byte-identical. No save change. +4 unit / 1 e2e. The precondition that gives directed carry (356), the both-zone readout (357), and edge barter (358) something to act on. (structure track)
- BACKLOG-355: Drew them across — the grove pull (345) is now *graded by how fresh the telling is*. A non-visited bowl dino just told to its face about the pond (the grove token sits among its most-recent memories) is dragged across ahead of one whose news has slid to the back of its 6-entry ring into mere ambient awareness. Pure `grovePull(events,visited,name,home) → 0|1|2` in curiosity.ts (`GROVE_TELL_RECENT` = 3); `groveCurious` reimplemented as `grovePull > 0` so the 345 predicate is preserved exactly; `pickMigrant` prefers a freshly-told dino (2) over an ambient one (1) over the old uniform random. `groveword.ts` is untouched — grove news stays the deliberate 1 hop (the cycle-75 contract holds); the strong/weak split rides recency, not a new propagation channel. No save change, NPCBrain intact. +5 unit / 1 e2e. (lore track)

## Cycle 077 — 2026-06-24
- BACKLOG-329: Carry between zones — the two per-zone economies (314/328) finally touch. A dino completing a *visible* crossing (334) ferries one banked resource from the pile it leaves into the pile it enters, the first trade-route link between bowl and grove. Pure `pickCarry(src,dest)` (the most-stocked kind the destination can still accept, deterministic) + `takeResource` in resource.ts; `crossDino` moves it via `takeResource` + the existing `bankResource`, conserved (source −1 / dest +1), lossless (empty source or capped destination carries nothing), with a glyph log. The instant `__migrate`/relocate teleport carries nothing, so every prior migration & zone spec stays green. No save change. +7 unit / 1 e2e. Unblocks directed carry (356) + edge barter (358). (structure track)
- BACKLOG-346: Pond-swappers — once two dinos have *both* set foot in the grove, meeting back in the bowl they trade pond notes: a small shared-place bond (`POND_BOND`) + a `🌿 traded pond stories with <other>` memory each, the grove's version of stargazing companions (288). Pure `pondSwap`/`pondSwapMemory`/`POND_BOND` in groveword.ts; `pondSwapBeat` runs in `converse` after the sympathy/grateful block (additive — the cold/grove cascade is byte-identical otherwise). The swap memory is unit-pinned clear of `GROVE_NEWS_TOKEN` so it never re-spreads as grove news. No save change, NPCBrain intact. +5 unit / 1 e2e. Book tie / own-voice / stronger-pull follow-ups = 353/354/355. (lore track)

## Cycle 076 — 2026-06-24
- BACKLOG-328: Per-zone stockpile — the shared park pile (285) split into one per zone, so each zone banks, caps (309), and spends its own gathering and a grove branch no longer lands in the same total as a bowl one. `stockpile` → `stockpileByZone` + `pileFor(zone)`; `checkGather` banks/caps/builds against the gatherer's home zone; the plaque Stores line follows the keeper's active zone. The pure `resource.ts` pile helpers reused unchanged; additive `stockpileByZone` save (validated one level deeper, legacy `stockpile` kept = bowl pile, old global pile migrates → bowl on restore), no version bump. +3 unit / 1 e2e. Unblocks carry-between-zones (329) + resource bias (348). (structure track)
- BACKLOG-345: News pulls a newcomer — grove news (342) now *moves a body*: the ambient migration roll prefers a grove-curious dino (a bowl dino that has only heard about the pond, never crossed) over a coin-flip, so word of a place draws the un-traveled to it. Pure `world/curiosity.ts` (`groveCurious`) + a `pickMigrant()` that falls back to uniform random when none is curious (existing migration specs untouched); one-time by construction (crossing → `groveVisited`). No save change, NPCBrain intact. 4 unit / 1 e2e. (lore track)

## Cycle 075 — 2026-06-24
- BACKLOG-316: Zone indicator — the plaque gains a per-zone tally line (`Zones · ▸Pocket Cretaceous N · The Grove M`): each zone's name + resident count, with a `▸` on the keeper's active zone, so the two-zone world the last seven cycles built is legible without walking it. Pure `zonePopulations` (the counting twin of `occupiedZones`) + `zoneTallyLine`; an additive `zoneTally?` on `PlaqueStats` (extends `plaqueLines` like the stores line); the marker moves on a crossing, counts update the instant a dino migrates. No save change. +5 unit / 1 e2e. (structure track)
- BACKLOG-342: Tell of the grove — a dino freshly back from the grove leads its next meeting with what it saw over there, so news of the second zone travels the bowl the way word of a cold night does. A grove→bowl return files a shareable `🌿 saw the pond over in the grove` memory; the existing relief→warm→cold word cascade gains a **grove** rung between cold and generic gossip (a worry still outranks scenery) that spreads it 1-hop. Pure `world/groveword.ts` cloned from `spreadColdWord`; reuses `RUMOR_MARK`/`isShareable`; no save change, NPCBrain untouched. 7 unit / 1 e2e. (lore track)

## Cycle 074 — 2026-06-24
- BACKLOG-315: Dino-built shelter — the build arc's second, larger structure beyond the cairn. Once a zone has stacked 3 cairns it stops draining the shared pile on cairns and *saves* toward a lean-to at the richer recipe {branch:6, stone:4} (the cairn auto-crafts at {3,2} every gather, so a count gate is the only way the pile climbs); the lean-to is placed/persisted/zone-scoped exactly like the cairn (additive `shelters` save, 🛖 glyph with the pixel prop seeded as 344), one landmark per zone, the cairn path untouched. 8 unit + 1 e2e. (structure track)
- BACKLOG-339: First steps in the grove — a dino crossing *into* the grove for the first time ever reacts: a 🌿 look-around bubble, a "first time across" memory (rides the existing store → can colour a later greeting), and a one-step pause before it wanders on. Pure `world/arrival.ts` (`firstGroveArrival`/`groveArrivalMemory`/`groveArrivalLine`); fires once per dino, grove-only; the `__migrate` teleport untouched. Additive `groveVisited` save, no version bump. 5 unit + 1 e2e. (lore track)

## Cycle 073 — 2026-06-23
- BACKLOG-334: Visible zone crossing — migration no longer teleports. The ambient roll marks a dino "crossing"; in `forceStep` it walks to its zone's linked edge (bowl→east, grove→west) via pure `migrationStepTarget`/`atMigrationEdge`/`crossEntryTile`, and on arrival flips its home zone + reappears at the far zone's opposite edge (like a keeper crossing). You see a dino leave for / arrive from the grove instead of blinking there. The deterministic `__migrate` hook stays instant (cycle-068/069/071 parity); no save change. 7 unit + 2 e2e. (structure track)
- BACKLOG-181: Sleep murmurs — a huddling, in-view dino floats a 💭 sleep-line drawn from its strongest day-memory (pure `world/murmur.ts`: most-recent memory → fragment, leading event glyph stripped); a memoryless dino dozes "💭 …zzz…". Sparse roll, deterministic (no model — the `NPCBrain` boundary is untouched), ephemeral (no save). LLM colour deferred to the 181 follow-ups (335–337). 5 unit + 2 e2e. (lore track)

## Cycle 072 — 2026-06-22
- BACKLOG-333: Realtime liveliness (operator-reported) — the bowl looked frozen and dinos never reached the grove because wander (every 5 in-game min) and migration (≤1/in-game day) were gated to the in-game clock, which at the 1× realtime default is glacial (5 real min/step; 1 migration/24 real hr). Wander now runs on a real-time timer (`WANDER_STEP_MS` 3 s) and migration on a real-time cooldown (`MIGRATE_COOLDOWN_MS` 60 s, pure `cooldownReady`), so the park mills + dinos cross zones at a watchable pace at any scale. `forceStep` body + `__migrate` untouched; no save change. Visible cross-zone walk = 334. (structure track)
- BACKLOG-325: Lingering lift — after a recovery flourish (318) a dino's idle quirk stays brightened for a short window (8 s) before settling to its signature; a sulk still wins the glyph. Pure render + transient window, no save/model. Smooth decay = 330. (lore track)

## Cycle 071 — 2026-06-22
- BACKLOG-318: Mood lifts the motion — the recovery twin of 310. When a funk clears (a sulk repaired (125) / a cold thawed (184)), the dino flashes a brightened flourish of its signature quirk (`🐾✨`), so bouncing back reads in motion, not just the mood emoji vanishing. Pure `reliefFlourish(p)` reused through the existing `flashFeed` channel at the two recovery seams, parallel to the repair/warm bubble; no affinity change, no save, no model. Lingering perk (325) + prickly-slower (327) deferred. (lore track)
- BACKLOG-314: Zone-aware resource spawn — resources go per zone. Each inhabited zone holds its own resource slot and rolls on its own cadence (pure `occupiedZones`), so the grove grows + holds its own gathering even while the keeper's in the bowl, ready (grace elapsed) the moment you cross. Gather/fetch/visibility run per zone; the 274/308 gate (gathered only in the keeper's active zone) is unchanged. `__resource`/`__spawnResource` kept backward-compatible; no save change. Per-zone stockpile = 328, carry-between-zones = 329. (structure track)

## Cycle 070 — 2026-06-22
- BACKLOG-310: Quirk shaded by feeling — a dino's signature idle fidget (298) now reads its transient mood, not just its temperament. A wandering dino that's the jealous runner-up awaiting a make-up greet (`pendingRepair`, 125) shows 😒 above its head in place of its usual quirk; the pure `moodFidget(p, mood?)` also shades the label ("paces, sulking" / "paces, shivering") for the readouts. No-mood is byte-identical to `fidget(p)` so every 298/303/306/312 path is untouched; cold keeps its existing floating 🥶 mark (no double glyph); no save, no model. Fed-😋 and book/scan/greeting wiring deferred to 318/319/321. (lore track)
- BACKLOG-309: Stockpile capacity + pressure — the shared per-kind resource stockpile (285) gains a cap (8/kind), the first economy constraint. `atCap` + a clamping `bankResource`; at cap a pickup is consumed but banks nothing and logs a stall beat (`🪵 stores full — Rex drops the branch`), so the pressure to spend on a craft (286) reads in-world; spending a kind relieves the cap. Lingering the capped resource on the ground was rejected (single-slot deadlock). No save change, `stockpileLine` byte-identical. Foundation for 315. (structure track)

## Cycle 069 — 2026-06-22
- BACKLOG-312: Quirk in the scan — LUMEN-3's Field Scan dossier now reports the dino's signature resting quirk as a `habit:` line (`habit: 🐾 paces`, `habit: 🫣 peeks around timidly`, …) alongside its axes, mood, role, and palate, read from the same deterministic `fidget()` the above-head glyph (298), the collection book (303), and the homecoming beat (306) use — so all four readouts of "who this dino is at rest" agree. Pure one-line add to `keeper/scan.ts`; no save, no model, no movement. (lore track)
- BACKLOG-308: Zone-scoped world objects — resources, cairns, and the plot now belong to a home zone and draw + interact only there, so the grove's own floor (294) is no longer overlaid with bowl-built props seen through the zone switch. A resource stamps the zone it fell in (drawn + gatherable only there — `checkGather` AND-gates `resource.zone` onto 274's `inView`); a cairn carries its crafter's zone in an additive save field (old saves backfill to the bowl, no `SAVE_VERSION` bump); the plot is a fixed bowl installation, workable in the bowl only. Object visibility re-applies on every zone cross + restore. 274 gated which *dino* is interactable; 308 gates where *objects* live. Per-zone spawn cadence (314) and structures beyond the cairn (315) are the next beats. (structure track)

## Cycle 068 — 2026-06-22
- BACKLOG-306: In-character homecoming — the welcome-back beat (112) now performs the returning dino's signature idle quirk: the closest dino greets you in its own body language ("Rex paces — You're finally back! 👋", "Twitch peeks around timidly — Oh — you're back. 👋"), reading the same `fidget()` label the book (303) and the live above-head glyph (298) use. Threaded via an optional quirk lookup so the no-lookup path is byte-identical (cycle-30 spec untouched); pure, no model, no save. (lore track)
- BACKLOG-274: Populate the grove — the grove (walkable since 143, its own terrain since 294) is finally inhabited. Dinos migrate between the bowl and the grove (a sparse real-time roll, capped ≤1/in-game-day, plus a deterministic dev hook), their home zone persists in the save (additive, old saves → all bowl), and the keeper can only greet/scan/feed the dinos in the zone it's standing in (the proximity filter on `nearestDino`/`checkFeeding`/`checkGather`). Spawn stays byte-identical — the grove fills by migration, not relocation. World-object zone-scoping (food/cairns/plot still draw at bowl coords) is deferred to 308. (structure track)
- BACKLOG-293: Crafted-object persistence — **ABANDONED as duplicate.** 286 already persists + re-renders the cairn via the additive `cairns` save field; three verdicts confirmed there's no remaining work. Closed so the queue stops carrying dead debt.

## Cycle 067 — 2026-06-21
- BACKLOG-303: Signature quirk in the dossier — the collection book now names each dino's idle fidget as a kept one-line fingerprint (`· paces`, `· peeks around timidly`, …), the label of the same `fidget()` quirk shown above its head while it wanders (298), so "who a dino is at rest" reads at a glance beside its hearts and role. Deterministic, no model, no save; `lenses.ts` stays Phaser-free. (lore track)
- BACKLOG-294: Grove terrain — the grove gets its own ground: a pure `groveTileAt` layout (a worn path band across the middle + a small NE pond over grass) and a per-zone floor swap that draws the grove under a cool tint, so the second zone reads as a different *place* instead of cloned bowl grass — and the long-benched path/water tile art (033) finally has sub-regions to land in. The path/water pixel rigs themselves stay the Artist's. (structure track)

## Cycle 066 — 2026-06-21
- BACKLOG-298: Idle fidgets — a goalless (wandering) dino shows its trait-derived signature quirk above its head instead of the generic 🚶 (bold→🐾 paces, timid→🫣 peeks, curious→👆 pokes the glass, …), keyed to its most-pronounced personality axis, so five idle dinos read as five individuals. Pure `world/fidget.ts`; the 295 `__activity` state is untouched, only the rendered glyph changes. No save. (lore track)
- BACKLOG-145: Plantable plot — one fixed plot the keeper plants with **P**; a 🍓 crop grows seed→sprout→ripe over realtime-clock days (read off the clock, no second timer); harvesting releases the crop as a food drop at the plot (reusing the feeding hatch, so the swarm + favorites loop apply) and bumps a persisted harvest tally. Plot + tally persist additively (no version bump). The build arc's growing half, beside its gathering half. (structure track)

## Cycle 065 — 2026-06-21
- BACKLOG-295: Dino activity readout — each dino carries a glyph for what it's doing now (✨ gazing / 👀 inspecting / 🆘 responding / 🍖 feeding / 💤 huddling / 🪵 gathering / 💬 socializing / 🚶 wandering), read off the same intent ladder `forceStep` uses, so the bowl reads as five busy individuals. Pure `dinoActivity`; live-derived, no save. (lore track)
- BACKLOG-297: Legible gathering — a freshly fallen resource lingers a grace window before any dino fetches it, a spawn note ("🪵 a branch fell") fires, and the spawn rate is bumped (0.05→0.12), so gathering is something the player actually catches instead of a sub-second blink. No save change. (structure track)

## Cycle 064 — 2026-06-21
- BACKLOG-288: Stargazing companions — when a sky event ends, every pair of dinos that watched it from adjacent spots (Chebyshev ≤ 1) gains a shared-wonder bond bump + a "watched the sky together" memory naming each other; a lone edge-watcher gets nothing. The collective awe (144/150) now also knits specific pairs. No save change. (lore track)
- BACKLOG-286: First craft — once the shared stockpile (285) covers the recipe (3 🪵 + 2 🪨), the dino that just banked stacks a cairn 🗿 in the bowl and the stockpile is spent by exactly that cost; cairns persist additively (no version bump) and re-render on load. The first resources→craft step of the civilization arc. (structure track)

## Cycle 063 — 2026-06-21
- BACKLOG-150: Stargazer's awe varies by temperament — during a sky event each dino presses in only to its own gaze ring (bold/curious crowd the gather tile, timid hang back at the cluster edge), so one collective beat reads five ways; all still file the shared memory. No save change. (lore track)
- BACKLOG-285: Resource stockpile — every 146 pickup banks into a shared per-kind park stockpile (`🪵 N · 🪨 N`), shown on the plaque and persisted additively (no version bump); the count 286/029 will spend. (structure track)

## Cycle 062 — 2026-06-20
- BACKLOG-278: Earned the nickname — at the heart cap (10) the closest dino drops the keeper's designation for its nickname ("There you are, Aki!"); 8–9 hearts still use the designation. The top rung of the greeting-by-relationship ladder. (lore track)
- BACKLOG-146: Resource gathering spine — a raw resource (🪵/🪨) appears in the bowl and a curious dino walks over and picks it up (per-dino tally, persisted). First beat of the resources→crafting→building arc; ranks below food + huddle. (structure track)

## Cycle 061 — 2026-06-20
- BACKLOG-276: The keeper has a name — a fond ≥8-heart dino names the chosen observer by designation ("There you are, AETHER-1!"); deep friendship earns your name in its mouth. (lore track)
- BACKLOG-040: Save format versioning + migration hook — `SAVE_VERSION=2` + a pure `migrate` chain so an older save is upgraded on load instead of discarded; the spine before 146/145/274 add more state. (structure track)

## Cycle 060 — 2026-06-19
- BACKLOG-272: Fond greeting from a close dino — a ≥8-heart dino opens warmly; completes the greeting-by-relationship spectrum (wistful ≤1 / generic / fond ≥8). (lore track)
- BACKLOG-032: Roles persist across cycles — `settleRole` makes an emerged role durable (never reverts to wanderer), persisted; jobs that stick. (structure track)

## Cycle 059 — 2026-06-19
- BACKLOG-271: Wistful greeting from a neglected dino — a ≤1-heart dino opens wistfully ("you came to see *me*?") instead of the generic hello; pure cannedReply branch + LLM clause keyed on affection, gratitude still wins. (lore track)
- BACKLOG-143: Connected zone — a walkable, persisted second zone (the grove) via edge-walk crossing; pure world/zones.ts + thin glue; grove starts empty (populate = -274); unblocks path/water art -033. (structure track — first parallel structure-track ship under CHARTER v5)

## Cycle 058 — 2026-06-19
- BACKLOG-261: Effusive thanks — the warm twin of grudging thanks (253). Cycle 57 voiced only the prickly pole of the cleared-name thanks (247); now warmth gets its own register too, so the same favour lands gruff from Rex, plain from the middle band, and gushing from a warm dino (`agreeableness > EFFUSIVE_MIN`): "<clearer> told the whole park I was alright — best friend a dino could ask for, I'll never forget it!" One branch in `thanksLine` between the gruff branch and the plain return, gated on a new `EFFUSIVE_MIN = 0.6` const pinned to `describePersonality`'s `> 0.6` high cutoff (the warm mirror of `PRICKLY_MAX = 0.4`, so the branches can't overlap); `buildMessages` folds the old `grudging` const into a three-way `manner` so the LLM path gushes too; "quietly" dropped from the grateful lead-in so it doesn't fight the gush (the `cleared your name` fact untouched). No WorldScene/world/save change — both greet sites already fed `gratitude` + `traits`; `cannedReply` already threaded traits, so the gush is free. Plain + gruff lines byte-identical, effusive branch fires only under `gratitude`, so cycle-055/056 regression specs stayed green; cycle-057's "warm = plain" assertions softened in-fire to "warm ≠ gruff". NPCBrain boundary clean, no deps, no save bump. 6 files (2 src, 2 new tests, 2 fixups); 9/9 AC; 518 unit / 183 e2e green, no flake (the one blip was the catalogued cycle-002 daynight parallel flake, green isolated). The gratitude manner is now a real three-way spectrum; heads 266–270 + 262/264/265.

## Cycle 057 — 2026-06-18
- BACKLOG-253: Grudging thanks — gratitude in each dino's own voice. The cleared-name thanks (247/251) was the same words from every dino; now it reads the `agreeableness` axis so a prickly dino (`< 0.4` — Rex/Mossback/Glade) grumbles it ("…yeah. thanks, I guess. Twitch set the record straight.") while a warm one (Sunny/Twitch) keeps the cycle-55 line verbatim. `thanksLine(clearer, traits?)` gains one optional arg + one gruff branch gated on a new `PRICKLY_MAX = 0.4` const pinned to `describePersonality`'s `low` cutoff; `cannedReply` threads `ctx.traits`; the `buildMessages` grateful clause appends a grudging instruction for prickly dinos so the LLM path matches. No WorldScene/world/save change (both greet sites already fed `gratitude` + `traits`); the grateful bond/memory/freshness untouched — only the wording moved. Warm line byte-identical to cycle 55, gruff branch fires only under `gratitude`, so cycle-055/056 regression specs stayed green. NPCBrain boundary clean, no deps. 4 files (2 src, 2 test); 9/9 AC; 508 unit / 182 e2e green, no flake. A "distinct minds" win; heads the 261–265 split.

## Cycle 056 — 2026-06-18
- BACKLOG-251: Gratitude fades — the spoken thanks (247) becomes a passing feeling, not a permanent script. `whoClearedMyName` (which drives both the canned thanks line and the LLM prompt weave) now scans only the freshest `GRATITUDE_FRESH_WINDOW` (3) entries of the memory ring instead of the whole ring, so once a just-cleared dino has lived on and filed that many newer memories, the clearing falls out of its freshest thoughts and the greet returns to a normal hello — the bond 243 earned stands, the memory may still ride the ring, the dino just stops bringing it up. One exported const + one loop-bound change on the existing parser; both spoken paths quiet together with zero WorldScene/brain edits (the gate is inherited at both call sites). Freshness from ring position, not a clock — no per-entry timestamp, no save change. The first freshness gate the cold/relief arc has shipped (222/233/244 still open can copy it). 9/9 AC; 500 unit / 179 e2e green, no flake. Unblocks 252/254/256-260.

## Cycle 055 — 2026-06-17
- BACKLOG-247: Thanks in the voice — a just-cleared dino names who cleared its name in its next keeper greeting (deterministic canned line + LLM-colour prompt weave, off the grateful memory cycle 54 already files). Pure `whoClearedMyName` parser + one `NPCContext.gratitude` field; no save change, no deps. 494 unit / 178 e2e green.

## Cycle 054 — 2026-06-16
- BACKLOG-243: Grateful to the one who cleared your name — the giving side of relief. Where the sympathy visit (217) turned a *worry* into a deed, this turns the *all-clear* into a bond: a recovered sufferer, meeting the dino carrying its first-hand relief memory (`saw <X> came through it fine`), warms to that clearer — a `GRATEFUL_BOND` bump (pinned `=== COMFORT_BOND`, as `SYMPATHY_BOND` is), a 💛 beat, and a "<clearer> cleared my name" memory. `world/cold.ts` gains the grateful trio + `clearedName`, a pure detector mirroring `sympathyVisit`/`selfCorrect`; its `isShareable` guard means only the holder of the *witnessed* relief counts — a downstream hearer of the 1-hop relief rumor does not. The converse seam adds one `else if` rung above the sympathy block (precedence 234 > 243 > 217), on the pre-meeting snapshot so a relief filed this meeting can't grant gratitude until a later one; byte-equivalent when it doesn't fire, so the cycle-049/050/051/052/053 seam pins stayed green. No new memory primitive (reuse held), no save change, no deps, boundary clean. 9/9 AC; 485 unit / 176 e2e green, no flake.

## Cycle 053 — 2026-06-15
- BACKLOG-235: Relief travels too — the retraction becomes news. A dino that dropped a stale cold rumor (234) files a first-hand relief memory (`saw <X> came through it fine`) and now leads with that all-clear the next time it speaks — a 1-hop rumor on the gossip spine, so a third dino never near the sufferer learns the worry is over. `world/cold.ts` gains the bright twin of the cold/warm word: `RELIEF_NEWS_TOKEN`, `reliefWordLine` (strips the `saw `, prefixes `RUMOR_MARK` so it can't re-spread), and `spreadReliefWord` (a structural twin of `spreadWarmWord`). The converse seam prepends a relief rung to the cascade (`relief ? : warm ? : cold ? : gossip`) with a distinct 😌 all-clear log; warm/cold/generic become `else if`, byte-equivalent when no relief memory exists, so the cycle-051/050/049 pins stay green. No new memory primitive (reuse held); same-meeting self-spread impossible by construction (relief filed after the cascade). No save change, no deps. 9/9 AC; 477 unit / 174 e2e green, no flake.

## Cycle 052 — 2026-06-15
- BACKLOG-234: The bowl self-corrects — recovery un-tells the rumor. A dino carrying word that a friend slept cold, meeting that friend and finding it recovered (it now carries a first-hand warm memory, 184), drops the now-false worry with relief ("Oh — you're alright now! 😌") instead of pitying it: it forgets the stale `coldWordLine(sufferer)`, files a relief memory, logs a 😌 line, and the cycle-217 sympathy visit is suppressed (bond untouched). A `selfCorrect` detector mirrors `sympathyVisit`; `recovered` reuses the exact `spreadWarmWord` predicate; the one new primitive is a four-line pure `forget` in `ai/memory.ts`. The seam checks self-correct first; the sympathy block is byte-unchanged in the else, so a non-recovered sufferer matches cycle 50. No save change, no deps. 9/9 AC; 471 unit / 172 e2e green, no flake.

## Cycle 051 — 2026-06-15
- BACKLOG-223: Word of the warmth — the gossip channel learns to carry good news. A dino the keeper warmed (184) now leads with that when it next meets another — "the keeper came for them, warmed them right out of the cold" — a 1-hop rumor on the same spine as the cold word (185), with a distinct 😊 log register. A warm-word trio (`WARM_NEWS_TOKEN`/`warmWordLine`/`spreadWarmWord`) mirrors the cold-word trio symbol-for-symbol; the converse seam becomes a three-tier fallback (warm → cold → generic) with warm checked first, because `warmMemory()` contains "cold night" and a rescued dino should talk about the rescue, not the cold. Byte-identical when no warm memory is present, so the cold-word + gossip + sympathy pins stayed green. No save change, no deps. 9/9 AC; 462 unit / 170 e2e green, no flake.

## Cycle 050 — 2026-06-15
- BACKLOG-217: Secondhand sympathy spurs a visit — the rumor mill's first feedback loop into behavior. When two dinos meet and one already carries the other's cold word from a prior meeting, the carrier crosses the bowl to keep the sufferer company: a sub-floor bond bump (`SYMPATHY_BOND` = `COMFORT_BOND`, the 130 console magnitude) + a distinct "came to find me after hearing" memory on the sufferer + a 🫂 step/line/log. Ignores the comfort bond-floor — hardship news, not closeness, moves the visitor. Detection reads a pre-meeting memory snapshot so this meeting's fresh word can't self-trigger; gossip plant byte-unchanged (cycle-020/049 pins green). Once-per-sorrow re-fire deferred to 226. No save change, no deps. 9/9 AC; 453 unit / 168 e2e green, no flake.

## Cycle 049 — 2026-06-14
- BACKLOG-185: Word of the cold — the bowl's first travelling news. A dino that slept cold now leads with the news when it next meets another, planting a distinct "the frost got into their bones — slept the whole night alone" rumor (a 🥶 log line, not the generic 🗣️) instead of whatever it remembered most recently. Reuses the whole cycle-20 gossip spine — the cold word carries `RUMOR_MARK`, so it's one hop and non-re-shareable for free; generic gossip byte-unchanged. No save change, no deps. 8/8 AC; 438 unit / 164 e2e green.

## Cycle 048 — 2026-06-13
- BACKLOG-208: Nobody came — the silent dusk thaw cycle 47 left open now leaves a mark: a cold-funked dino the keeper never warmed files a colder "shivered all morning; nobody came 😞" memory before the funk clears, compounding with the morning's cold note and tinging its next greeting harder. A warmed dino is spared it. Neglect made as legible as care, in six lines on one existing branch — no new listeners, no save change. 10/10 AC; 424 unit / 160 e2e green.

## Cycle 047 — 2026-06-12
- BACKLOG-184: Keeper's warmth — the cold night becomes mendable: cold sleepers carry a visible 🥶 funk until the keeper greets or feeds them warm (an outsized repair-shaped gain + a "the keeper warmed me" memory that colours tomorrow's hello), or dusk thaws it silently. No save change. 10/10 AC; 410 unit / 156 e2e green.

## Cycle 046 — 2026-06-12
- BACKLOG-194: Distress call — the first time sound moves a dino: a startled bolter or the loneliest cold sleeper cries out in its own voice raised to a distress register, and its closest friend (the cycle-33/34 consolation rules, reused verbatim) turns 👂, remembers, and walks toward the cry; a friendless cry hangs unanswered. Diegetic — mute silences playback, never the bowl. No save change. 10/10 AC; 395 unit / 149 e2e green.

## Cycle 045 — 2026-06-12
- BACKLOG-192: Dawn chorus — at the live dawn boundary the cast greets the day each in its own cycle-44 voice, staggered by energy (eager risers first, night-owls last); pure `audio/chorus.ts` + a live-only `onHour` seam, once per in-game day, mute-safe, no save change. 10/10 AC; 375 unit / 137 e2e green.

## Cycle 044 — 2026-06-11
- BACKLOG-191: Audio spine — after 44 silent cycles the bowl speaks: every dino has a synthesized voice derived from its own traits (solitary+timid squeaks high, social+brave rumbles low — founders spread 148→797 Hz untouched by hand), chirping on every greet and dino↔dino line; the glass rap thunks; M / 🔊 mutes, persisted. Pure WebAudio synthesis, no assets, autoplay-safe, zero save change. The operator's Idea-Box nudge, shipped the same day it was dropped.

## Cycle 043 — 2026-06-11
- BACKLOG-179: Cold-night shiver — a dino too loosely bonded for the winter den shivers 🥶 at morning and files a "slept cold, alone" memory that colours its next greeting; winter only, the warm seasons inert. The other half of cycle-42's packing den: who sleeps alone in winter is now a beat, not a still picture.

## Cycle 042 — 2026-06-10
- BACKLOG-171: Winter huddle pull — season-conditional den rules (winter 4 pts / 19→7 packs the den from dusk; summer 8 / 23→4 scatters warm nights; spring byte-identical legacy); pure world/huddle.ts, zero save change.

## Cycle 041 — 2026-06-10
- BACKLOG-170: Seasonal palates — the turning year (159) finally reaches into the loop the player drives daily: feeding. Each season craves one food (spring → 🌿 greens, summer → 🍓 berries, fall → 🐟 fish, winter → 🍖 meat) and lends it a small **+0.4** nudge inside the favorite-food verdict. The bonus is deliberately tiny — it only ever flips a dino whose top-two foods are near-tied, and it can *only* promote the craved food, never reorder the rest. Sized against the actual name-seeds, the cast splits itself into character: **Rex, Mossback, and Sunny each sway with the year** (and each toward a different food), while **Twitch the herbivore (a 1.57 fit gap) and Glade the carnivore never budge** — "who follows the season and who's loyal" becomes a free new read on personality. The same hatch drop now reads the calendar back to you: drop meat in winter and a meat-craver comes running and eats it as its *favorite* (😋 + a "favorite" memory); drop the same meat in summer and it's plain feed to that same dino. The whole thing is **derived state** — `season` is an optional argument on `favoriteFood` / `foodReaction` / `scanLines`, so omitting it is byte-identical to cycle-061 and every prior food/scan test stayed green untouched; the save format grew by **nothing**. The feeding rush, the eat reaction, the `__favoriteFood` hook, and LUMEN-3's Field Scan all read the *same* live clock season, which is exactly why the cycle-027 favorites e2e still passes — the food dropped and the food eaten are judged in one season by construction. Pure `world/foods.ts` (the craving table + the bonus); WorldScene only passes the live season in (`currentSeason()`); new `__seasonCraving` dev hook. No new keys or deps; NPCBrain boundary intact; day/night + season-tint rendering untouched. 7 new unit + 4 new e2e; 8/8 AC pass; 298 unit / 105 e2e green (the lone red was the documented cold-boot parallel flake, green isolated in 939ms). Unblocks 176 (seasonal gift cravings) and the rest of the cycle-41 lore set (174/175/177/178).

## Cycle 040 — 2026-06-09
- BACKLOG-159: Season foundation — the bowl gets a **turning year**. Seasons run a week of in-game days each (spring → summer → fall → winter, a 28-day year that wraps forever), shown on the clock HUD (`Day 9 — 08:00 ·1× · summer`) and washed over the world as a subtle per-season tint (≤ 0.12 alpha, depth 4) under the day/night overlay — the year colours the bowl, the sun still owns the light. When the year *actually turns* under a live tab, the bowl marks it once: a fading banner ("The season turns — winter pales the light in the bowl."), a 🍂 line in the Park News ticker, and **every dino files the memory** — so the turn is gossip fuel and prompt context the moment 173 lands. The season is **pure derived state** from the clock day the save already persists: the save format grew by nothing, and boot / restore / away fast-forward are all proven silent (restore re-derives, never beats). The Code-planner caught the trap that would have gutted the e2e: a 7-day `__advanceWall` takes the clock's MAX_CATCHUP `set()` branch and fires no listeners, so the test stages day 7 23:59 via a restore-semantics `__setClock` (which *cannot* fake a turn) and crosses midnight with a 2-minute live advance. Pure `world/seasons.ts`; WorldScene glue only. No new keys or deps. 7 new unit + 4 new e2e; 9/9 AC pass + a bonus restore-guard e2e; 283 unit / 99 e2e green, full e2e first try. Spine for festivals (026), weather (028), and the cycle-40 lore items (170–173).

## Cycle 039 — 2026-06-09
- BACKLOG-161: First-contact inspection — the bowl finally *answers* a keeper pick. The moment you switch observers, the dino whose temperament most resonates with the new watcher drops what it's doing and crosses the bowl for a long look: a 👀 beat at arrival plus a persisted "went to the glass for a long look at VANTA-9 'Vix'" memory it can carry into later lines. Who comes is a read on who you became — Vix draws Glade, Lux draws Rex, Aki draws Sunny — and an observer nobody resonates with draws *no one*, which is its own kind of telling (strictly-positive fit required, the comfort-floor grammar). Re-picking your current observer, booting, or restoring a save arms nothing: only a real change of watcher turns heads. Pure `keeper/firstContact.ts` (positive argmax over `keeperFit`, alpha tie-break, INSPECT_TTL 24 — the Code-planner caught that the design's 12 could expire before a worst-case ~19-tile walk, and a unit test now pins TTL ≥ 19); WorldScene glue only (changed-id arming in the picker, a one-dino beeline override in the movement loop, a once-per-step resolver on the `pendingRepair` one-shot pattern). No save-format change; no new keys or deps; NPCBrain boundary intact. 6 new unit + 4 new e2e; 9/9 AC pass; 276 unit / 95 e2e green — full e2e first try. Unblocks 165 (watcher gossip) / 167 (the unimpressed).

## Cycle 038 — 2026-06-09
- BACKLOG-157 (first of three abilities; item stays open): **Field Scan** — LUMEN-3 "Lux" earns its Scholar Lens. Playing Lux, press **B** beside a dino and a dossier opens: all five personality axes drawn as ▮▯ meters between their pole labels (cautious↔curious, prickly↔warm…), current mood, favorite food, and emergent role — the hidden state that until now only the code could see. Playing AETHER-1 or VANTA-9, the same key gets you a refusal *in that observer's own voice* ("A diplomat does not pry into a mind…") — who you chose now gates what you can know, not just who warms to you. Pure `keeper/scan.ts` (canScan / scanLines / scanRefusal — pure formatting over `AXES`, `moodFromTraits`, `favoriteFood`); WorldScene glue only (B toggle, panel, non-modal refusal bubble, dev hooks incl. a new `__warpTo` test warp). No save change; no new deps; NPCBrain boundary intact. 7 new unit + 4 new e2e; 10/10 AC pass; 270 unit / 91 e2e green (first-run failures were the documented cold-boot flake — green isolated + on the fresh full run). The Designer overrode the lore handoff's bond-sight suggestion on prior-art grounds (the V bonds lens already shows everyone the graph) — the chain catching its own duplicate. Remaining: Vix's and Aki's powers, one per cycle.

## Cycle 037 — 2026-06-09
- BACKLOG-155: Selectable keeper — the faceless yellow square becomes someone you *choose*. The player is now one of three **time-traveling robot observers** — AETHER-1 "Aki" (Empath Protocol), VANTA-9 "Vix" (Daredevil Drive), LUMEN-3 "Lux" (Scholar Lens) — each with its own era, a one-line backstory, and a distinct ability. Press **K** to open a picker (1/2/3 to choose, styled like the tone menu); the choice **persists** across reloads and into the exported save. The shipped ability is a personality **fit**: the chosen observer adds a small affinity bonus (0..+2) when greeting/gifting dinos whose temperament matches it, so *which* observer you are quietly decides *which dinos* warm to you fastest — the same choice reads differently against each distinct mind. Pure `keeper/keepers.ts` (`KEEPERS` roster + `keeperById`/`keeperFit`/`keeperBonus`, mirrors `social/tones.ts`); WorldScene glue only (the `K` picker overlay, a number-key dispatcher so 1/2/3 routes to the open overlay, the bonus applied at the `recordGreet`/`recordTone`/gift seams, a non-blocking fading first-time invite — no modal on boot). Additive `keeperId?` save (old saves + fresh games default to AETHER-1; no `SAVE_VERSION` bump); no new deps; NPCBrain boundary intact. 9 new unit + 4 new e2e; 12/12 AC pass; 263 unit / 87 e2e green. QA caught a stale tones assertion (the keeper bonus correctly colors the tone-pick greet) and fixed it in-session. First beat of the keeper arc (the operator's live-session ask); unblocks 156/157/158.

## Cycle 036 — 2026-06-08
- BACKLOG-144: World-scale night event — the bowl's first *collective* beat. For thirty-five cycles every reaction was pairwise; now, on a rare clear night, the sky lights up (a **meteor shower** or an **aurora**) and the **whole cast** abandons its wandering to drift to one spot near the centre of the bowl, gather, and gawp — each dino throwing a ✨ awe bubble and filing **one shared memory** ("the whole sky rained falling stars, and we all watched it together"). The spectacle fades a shimmer overlay over the night tint and ends when its duration elapses or night passes into dawn, after which ordinary life resumes. Pure `world/skyEvent.ts` (`SKY_EVENTS`, `pickSkyEvent`/`rollSkyEvent`, `atGather`, `skyExpired`, gather tile 10,7 — mirrors `comfort.ts`); WorldScene glue only (shimmer overlay at depth 7, a top-priority `stepSky` override in `forceStep`, dev hooks `__skyEvent`/`__triggerSky`/`__skyGazers`). The auto-trigger rolls on a **real-time cadence** (not in-game hours, so offline catch-up never conjures one) and is **capped at one per in-game day** — genuinely rare. No save-format change at all (the shared awe rides the existing persisted memory store); no new deps; NPCBrain boundary intact. 7 new unit + 4 new e2e; 9/9 AC pass; 254 unit / 83 e2e green. QA caught a cadence regression pre-ship (the roll was first on `onHour` and dragged the cast off the den in the huddle test); fixed in-session. Unblocks 150–154.

## Cycle 035 — 2026-06-08
- BACKLOG-142: Player dialogue tones — the first two-way beat in player↔dino dialogue. Greeting a dino (E/Z) now opens a **Warm / Tease / Honest** menu instead of an instant reply; 1/2/3 picks a tone, each dino weighs it from its name-seeded personality (warm dino loves Warm, bold-prickly enjoys Tease, timid bristles), so the same gesture lands differently per dino. The pick applies a personality-fit affinity delta (`loved +5 / liked +3 / neutral +1 / clashed −2`), files a `the keeper …` memory, and persists a **last-tone trace** the menu reads back to you next time ("Last time you were warm with them."). Pure `social/tones.ts` (mirrors `gifts.ts`: `TONES` + `toneScore` + `toneReaction` + `lastToneLine`); WorldScene glue only (`recordTone`, a repair-seam-preserving twin of `recordGreet`); reply path unchanged (tone-coloured replies deferred to BACKLOG-148); additive `lastTone` save field (no `SAVE_VERSION` bump). 9 new unit + 4 new e2e; 9/9 AC pass; 243 unit / 77 e2e green (isolated). Opens the dialogue-choices arc (148/149); first beat seeded from the Idea Box's first firing.

## Cycle 034 — 2026-06-07
- BACKLOG-132: Gratitude echo — the bond graph (BACKLOG-013) bends back the other way. A dino consoled in a comfort beat (BACKLOG-130) files *who* came for it in a persisted, additive `gratitude` ledger (`consoled → comforters`); on a later homecoming where that comforter is itself the sulking runner-up (BACKLOG-120), the dino it once consoled crosses the bowl **first** — past a stronger-bond peer, ignoring `COMFORT_BOND_FLOOR`. Reciprocity, not just proximity. Pure `world/comfort.ts` (`Gratitude`, immutable+deduped `recordGratitude`, a reciprocity override on `comforter` evaluated before the unchanged closest-friend scan with the same alpha tie-break); WorldScene glue only; `homecoming.ts` + the keeper-repair seam (125) untouched; reward currency stays the dino↔dino bond; additive save (no `SAVE_VERSION` bump). 15 new unit + 2 new e2e; 9/9 AC pass; 231 unit / 73 e2e green. Unblocks 137/138/139/140/141.

## Cycle 033 — 2026-06-06
- BACKLOG-130: Comforting nuzzle — the dino-to-dino half of the attention economy. When a homecoming makes a near-tied runner-up sulk `😒` (BACKLOG-120), the sulker's *closest friend* (highest pairwise bond, BACKLOG-013) crosses the bowl and consoles it: a floating `There there, <sulker>. 🫂`, the pair's bond grows by `COMFORT_BOND` (+2), and the slighted dino keeps a "<friend> came over to comfort me" memory. A poorly-integrated dino with no bond above `COMFORT_BOND_FLOOR` (8) gets no one — telling in itself. Pure `world/comfort.ts` (`comforter` w/ floor + alpha tie-break, `comfortLine`, `comfortMemory`); WorldScene glue only; `homecoming.ts` + the keeper-repair seam (125) untouched; reward currency is the dino↔dino bond, not player points; additive save. 7 new unit + 2 new e2e; 9/9 AC pass; 212 unit / 70 e2e green. First beat to read the long-dormant bond graph; unblocks 132/133/136.

## Cycle 032 — 2026-06-05
- BACKLOG-125: Greeting the runner-up — the repair half of the jealousy keystone. A homecoming's near-tied runner-up (the dino that sulks `😒` in BACKLOG-120) is marked pending-repair; greeting it flips the slight with an outsized bump (`greetGain` +6), a floating `You noticed me! 😊`, and a "the keeper noticed X after all" memory. One-shot per slight, transient (no save change). Pure additive `world/repair.ts`; WorldScene glue only; `homecoming.ts` untouched. 5 new unit + 3 new e2e; 9/9 AC pass; 202 unit / 67 e2e green. Closes the hurt→heal loop; unblocks 128/129.

## Cycle 031 — 2026-06-04
- BACKLOG-120: Jealous nuzzle — when a homecoming fires, a near-tied runner-up (within 10 pts / one heart of the closest) sulks `Hmph. 😒` alongside the 👋 and keeps a "the keeper fussed over X" memory. Pure additive `jealous` field on `homecoming.ts` (generalized `closest`→`topBy(exclude?)`, shared alpha tie-break); WorldScene glue only; no friendship-points change; additive save. 8 new unit + 2 new e2e; 9/9 AC pass; 193 unit / 63 e2e green. Keystone for 123/125/126.

## Cycle 030 — 2026-06-03

- BACKLOG-112: Homecoming nuzzle — APPROVED. After a long real absence, your *closest* dino (highest player-friendship) notices you came back: a heart-graded 👋 "welcome back" bubble floats over it on return, and it keeps a faint "the keeper came home" memory. Reads the BACKLOG-106 catch-up duration; gated at 6 in-game hours so an instant reload stages nothing. Pure `world/homecoming.ts` (max-friendship + alphabetical tie-break, three warmth bands) with WorldScene glue only (restore + `__catchUp` compute, `showBubble`, `remember`); reuses `friendship.heartsFromPoints` + `memory.remember`. No friendship-points change (hearts ACs untouched); additive save, boundary intact. 8 new unit + 2 new e2e; 185 unit / 61 e2e green. First personal beat on the cycle-29 spine; unblocks 119–122.

## Cycle 029 — 2026-06-02

- BACKLOG-106: Offline catch-up ("while you were away") — APPROVED. On load, reads the real gap since `savedAt` (× the saved scale) and fast-forwards the world cheaply — no per-tick loop, no LLM: bonded companion pairs drift closer (capped), each gets a "kept each other company" memory, and a "While you were away…" digest greets the player. Pure `world/away.ts` (`awayMinutes`, `fastForward`) + a new `advanceTime` clock export; span capped at 7 in-game days so a week away can't hang the load. Additive save (no version bump); old saves no-op the catch-up. 13 new unit + 2 new e2e; 170 unit / 58 e2e green. The payoff of cycle-28 realtime; spine for the 112–116 cluster.

## Cycle 028 — 2026-06-01

- BACKLOG-105: Wall-clock-anchored time + configurable scale — APPROVED. The clock no longer counts timer ticks; it reads the wall clock. `WorldClock` now anchors an epoch + an absolute in-game minute and derives the current `GameTime` from `(now() − anchor) × scale`, via an **injectable** `now()` source so it stays pure/Node-testable. `tick()` survives untouched as the one-minute boundary primitive — the new `update()` pump just calls it for each whole minute the wall clock has crossed — so every prior time-driven spec and `__advanceMinutes` keeps working. The payoff: a backgrounded tab (where Phaser throttles) catches up to true time on return instead of silently falling behind. Default is **1× realtime** (a full day/night = 24 real hours — the true fishbowl); **T** toggles 1×⇄60× (the old "1 real sec ≈ 1 game min" watching rate) and re-anchors so the time never jumps on toggle; the HUD shows the active rate (`Day 1 — 08:00 ·1×`). Catch-up is capped at one in-game day of per-minute fires — a longer gap jumps the clock without flooding listeners (rich offline catch-up is BACKLOG-106). `SaveData` gains additive `savedAt`/`scale` (the seed 106 needs); `SAVE_VERSION` stays 1 and old saves still load. Pure `world/clock.ts` + additive `world/saveGame.ts`; WorldScene glue only (T key, HUD, hooks). 9 new unit + 2 new e2e; full suite **157 unit / 56 e2e** green. Boundary clean (AI untouched). Unblocks BACKLOG-106 (offline catch-up) and the cycle-28 realtime rituals (108–111).

## Cycle 027 — 2026-06-01

- BACKLOG-061: Food favorites — APPROVED. The hatch feed has a flavor now and the dinos have opinions. **H** drops one of four foods (🍖 meat, 🌿 greens, 🐟 fish, 🍓 berries) and each dino quietly favors one — derived from its temperament with the *same* `giftScore` math the gift system uses (a food is a gift through the lid). When a dino snaps up *its* favorite it's extra-happy: a bigger bump (`FEED_GAIN_FAV=9` vs the plain `FEED_GAIN=5`), a 😋 (vs 🙂), and a "your favorite …!" memory it can later gossip about; it also rushes its favorite harder — a wider reach (`FEED_RANGE_FAV=12`) and a lower energy bar (`EAGER_FAV=0.15`) — so the keeper learns each palate by watching who comes running for what. Favorites vary (Rex/Glade → meat, Mossback/Sunny → berries, Twitch → greens). Pure `world/foods.ts` (`favoriteFood`, `foodReaction`) + an optional `isFavorite` on `feeding.ts`'s `reactionToFood` (default false → cycle-25 behavior byte-identical); WorldScene glue only. 9 new unit + 2 new e2e; full suite **148 unit / 54 e2e** green. Save unchanged (favorites re-derive from the name; food stays ephemeral). Opens the taste cluster: 066 taste talk, 067 keeper-loaded hatch, 068 acquired taste, 069 menu-in-the-book, 070 picky/gobble.

## Cycle 026 — 2026-05-31

- BACKLOG-060: Idle / ambient mode — APPROVED. The vivarium becomes a quiet desktop companion: after `IDLE_AFTER_MS=12s` with no input, the HUD eases down to `AMBIENT_ALPHA=0.12` over `FADE_MS=1.5s` and the camera starts a slow "breathing" zoom (1.0↔1.04, 6s yoyo) toward the centre of the bowl — leaving just the glass and its life. Any key, click, or held movement snaps everything back to full instantly. Held WASD counts as activity (movement polls `isDown` and doesn't refire keydown). Pure `world/idle.ts` (`isIdle`, `hudAlpha` — monotonic fade curve); WorldScene tracks `lastInputAt`, fades a set of always-on HUD texts, and runs the camera tween. Completes the fishbowl furniture (056 glass / 057 tap / 058 plaque / 059 feed / 060 idle). 5 new unit + 2 new e2e (fade + ambient flag on idle; real keypress wakes it); full suite **139 unit / 52 e2e** green. Save unchanged.

## Cycle 025 — 2026-05-31

- BACKLOG-059: Feeding hatch — APPROVED. The bowl's lid has a hatch now: press **H** and a 🍖 falls from the top into the upper-middle feeding zone, and the cast swarms it. Eager, in-range dinos (`energy ≥ 0.4` within 7 tiles) make a beeline; the calm and the far-off keep wandering. The first dino to reach the food snaps it up — 😋, a friendship bump (`FEED_GAIN=5`), and a "scrambled to the hatch and snapped up the food" memory that can ripple into gossip; the rest disperse. One piece at a time; the drop + eat both post to the Park News ticker. Reframes gifting (F, hand-to-one) as feeding the tank (H, broadcast-to-cast) — the keeper picks the input, the bowl picks the winner. Pure `world/feeding.ts` (`reactionToFood`/`feedStep` reusing `stepToward`/`reachedFood`/`foodLanding`); WorldScene glue only. 9 new unit + 2 new e2e; full suite **134 unit / 50 e2e** green, no flake. Save unchanged (food is ephemeral). Spine for 061 favorites / 062 scramble / 063 begging / 064 hoarder role / 065 feed-log.

## Cycle 024 — 2026-05-30

- BACKLOG-058: The Plaque — APPROVED. An engraved brass nameplate sits under the bowl — *"VIVARIUM · Pocket Cretaceous"* over a live stat line: `Day N · M specimens · G generations`. The generation count is a pure readout of the lineage the breeding loop produced: founders are gen 1, a hatchling is 1 + the deeper of its two parents, so the plaque deepens on its own as families breed across days. Pure `ui/plaque.ts` (`generationOf` memoized + cycle-guarded, `maxGeneration`, `plaqueLines` with pluralization); the scene renders it bottom-center, refreshed on the clock tick. 5 new unit + 1 new e2e (plaque reports population and ticks to 2 generations when an egg hatches); full suite **125 unit / 48 e2e** green. The bowl now wears its label.

## Cycle 023 — 2026-05-30

- BACKLOG-057: Tap the glass — APPROVED. The keeper can now rap the vivarium wall (click anywhere): a ripple ring expands at the tap and every dino within range reacts **by temperament** — the timid bolt away (❗), the bold creep closer to investigate (❓), the far-off don't notice. The reaction is decided by the dino's seeded `bravery` and its distance from the tap, and the scare is written into the dino's memory ("the glass shook and you bolted/crept closer") so it can ripple into gossip and dialogue. Pure `world/startle.ts` (`reactionFor`/`fleeStep`/`startleStep`, reusing `stepToward`); WorldScene adds a `pointerdown` handler, the ripple tween, and the ❗/❓ flash. 6 new unit + 2 new e2e; full suite **120 unit / 47 e2e** green. First time the keeper can *touch* the bowl.

## Cycle 022 — 2026-05-30

- BACKLOG-056: The Glass (vivarium frame) — APPROVED. Reframes the whole game per the operator's "mini dino fishbowl" idea: the world is now a **sealed glass vivarium** the player keeps and watches. Draws the bowl — an edge vignette (corners doubled for a deeper shadow), a crisp pale-cyan glass rim with a faint inner highlight, two reflection streaks, and a curved light-catch arc along the top lip. Pure geometry in `ui/glass.ts` (`cornerRadius`/`rimRects`/`edgeBands`/`glarePolys`/`toPoints`); the scene draws it at depth 8 (over the night tint + bond lines, under the HUD). No sim change — purely visual. New lore doc `studio/lore/vivarium.md` + a fishbowl backlog section (057 tap-the-glass, 058 plaque, 059 feeding hatch, 060 idle mode). 5 new unit + 1 new e2e; full suite **114 unit / 45 e2e**, green across two consecutive runs. Visually verified in a live preview.

## Cycle 021 — 2026-05-30

- BACKLOG-021 + BACKLOG-020: Observer lenses — APPROVED. One key (**V**) cycles the player through ways of *seeing* the emergent sim, each a pure readout of state the sim already produced (no new scripting): **📖 Collection Book** (every dino incl. hatchlings — species, hearts, strongest bond, emergent role, lineage "child of X + Y", rumors heard), **🔗 Bonds** (lines drawn between bonded pairs on the map, thicker = stronger), **🎭 Roles** (a role tag floats over each dino — `gossip`/`homebody`/`socialite`/`wanderer` **derived from behavior**, BACKLOG-020), **📰 Park News** (live ticker of births/hatches/gossip). Pure `ai/roles.ts` (`deriveRole`) + `ui/lenses.ts` (`nextLens`/`bondedPairs`/`tickerLines`/`bookLines`); `BornDino` gains a `parents` field for lineage (additive). WorldScene adds the lens layer + an event log. 14 new unit + 3 new e2e; full suite **109 unit / 44 e2e**, green across two consecutive e2e runs. Visually verified all four lenses in a live preview. Turns the invisible sim into something you watch — without ever authoring an outcome.

## Cycle 020 — 2026-05-30

- BACKLOG-019: Gossip propagation — APPROVED. When two dinos converse on meeting, the speaker passes a recent **first-hand** memory to the listener as second-hand news, planted in the listener's memory marked `told me:` so it won't re-spread (1 hop, no loops). Because memory already feeds the prompt as "Lately: …", a dino can now bring up things it only *heard* about — news ripples through the park as the cast mingles. Pure `social/gossip.ts` (`swapPronouns`/`isShareable`/`pickGossip`/`makeRumor`/`spreadGossip`), wired into the throttled `converse` path so it spreads at conversation cadence. 7 new unit + 2 new e2e; full suite **99 unit / 41 e2e**, green across two consecutive e2e runs.

## Cycle 019 — 2026-05-30

- BACKLOG-042: Egg phase — APPROVED. The park grows itself. When two dinos whose bond clears the threshold (`EGG_BOND_THRESHOLD=60`) share a sleeping huddle on a clear night, a 🥚 appears by the den; after `EGG_HATCH_DAYS=3` in-game days it hatches into a brand-new dino whose traits, color, and species are **blended from the two parents** (pure `breeding.ts`: `blendTraits`/`blendColor`/`childName`/`shouldLay`/`hatch`). Born dinos and pending eggs ride into the save (additive, version 1) and respawn on reload, so the family tree survives sessions. A hard `MAX_POPULATION=12` cap stops runaway breeding. Weather isn't in yet (BACKLOG-028) so every night counts as "clear". 12 new unit + 3 new e2e; full suite **92 unit / 39 e2e**, green across two consecutive e2e runs. Closes the social loop started by cycle-18 huddles: meet → bond → huddle → **breed**.

## Cycle 018 — 2026-05-30  (capstone)

- BACKLOG-013 + BACKLOG-041: Pairwise bonds + night sleeping huddles — APPROVED. Every NPC↔NPC meeting strengthens a symmetric pairwise **bond** (0–100, pure `bonds.ts`, persisted). A visible **den** sits lower-centre; at **night** any dino whose strongest bond ≥ threshold walks to the den instead of wandering, clusters with its friends (💤 over each), and the huddle adjacency keeps deepening the bond — then they disperse at dawn. Self-reinforcing: meeting builds bonds, bonds drive huddling, huddling builds bonds. 8 new unit + 2 new e2e; full suite 80 unit / 36 e2e, green across three consecutive runs.

## Cycle 017 — 2026-05-30

- BACKLOG-011: NPC memory store — APPROVED. Each dino keeps a small ring buffer of recent events (you greeting it, gifts + how it reacted, running into other dinos). The last few are woven into its prompt ("Lately: …") so it reacts to history, and the memory rides into the save — so a dino remembers you next session. At dawn each dino folds its events into a one-line reflection. Pure `memory.ts` (`remember`/`recall`/`reflect`); additive save field (version unchanged, old saves default to empty). 7 new unit + 1 new e2e.

## Cycle 016 — 2026-05-30

- BACKLOG-006: Device probe — APPROVED. The brain sizes the model to the device instead of hardcoding 0.5B: pure `pickTier` scores `navigator.deviceMemory` + the WebGPU adapter's max storage-buffer size into tiny (0.5B) / small (1.5B) / medium (3B), and `defaultLoader` loads that model; choice surfaced via `window.__modelLabel`/`__modelInfo`. (Browsers can't write `config.json` — selection is exposed instead.) 5 new unit + 1 new e2e.

## Cycle 015 — 2026-05-30

- BACKLOG-055: Livelier character voice — APPROVED. Diagnosed bland replies: the prompt threw away the vivid roster flavor (only dry trait adjectives reached the model), was negative-heavy, and the output was triple-truncated (one sentence + 60 tokens + first-sentence-only cleaner). Fix: the system prompt now leads with character and feeds BOTH the roster flavor ("loves rocks", "quick to bolt") and the trait adjectives, with a single light "never a chatbot or helper" clause and an invitation for "one or two vivid, specific sentences"; `cleanReply` keeps up to 2 sentences; generation is `max_tokens 100` / `temperature 0.9`. 66 unit / 32 e2e green.

## Cycle 014 — 2026-05-30

- BACKLOG-052: NPC↔NPC dialogue — APPROVED. Dinos now drift toward each other (~45% of steps via pure `stepToward`) so they actually cluster and meet, and on meeting one speaks a brain-generated line (`npc_meet`) in a floating speech bubble above it (🧠-tagged when model-written), throttled by a cooldown + in-flight guard to protect the single shared engine. Also added a WebGPU guard so the brain fails fast to the canned fallback (and skips a doomed worker) on browsers without WebGPU. 3 new unit + 2 new e2e.

## Cycle 013 — 2026-05-30

- BACKLOG-053: Sane controls — APPROVED. WASD movement (arrows kept), E to interact and F to give (Z/G kept as aliases), save-export moved off E to O, plus an on-screen controls hint. 2 new e2e.

## Cycle 012 — 2026-05-30

- BACKLOG-051: Richer dialogue context — APPROVED. The WebLLM prompt now carries the time of day (`dayPhase`), the dino's current mood (`moodFromTraits`), and the player's friendship level (hearts → `relationshipLabel`), and the greet line itself changes with the relationship (stranger → dear friend). Pure prompt enrichment reusing three existing signals; verifiable via the new `__greetPrompt` dev hook. 2 new unit + 2 new e2e; 7/7 AC pass. Directly targets the "mostly hellos" sameness.

## Cycle 011 — 2026-05-30

- BACKLOG-018: NPC movement + meeting spine — APPROVED. Dinos wander the map (pure `wanderStep`, clamped, driven off the world clock at a gentle throttle) and record a symmetric pairwise "meeting" tally when two end a step adjacent (pure `recordMeet` — the seed of pairwise affinity), flashing both labels. `nearestDino` reads live positions, so greeting/gifting follow a dino wherever it roams. LLM dino-to-dino dialogue deferred to BACKLOG-052. 5 new unit + 2 new e2e; 8/8 AC pass.

## Cycle 010 — 2026-05-30

- BACKLOG-049: WebLLM Web Worker offload + observability — APPROVED. Inference moved into a dedicated Web Worker via `CreateWebWorkerMLCEngine`, so model load/generation no longer block the render loop (build confirms web-llm split into its own worker chunk). Added a brain-status HUD (🧠 thinking… / ready / offline) and a per-reply `source` tag that prefixes 🧠 on lines the model actually generated — so the player can tell a real LLM line from the canned fallback. 2 new unit + 2 new e2e; smoothness/tag is a human WebGPU check.

## Cycle 009 — 2026-05-30

- BACKLOG-015: Gift system — APPROVED. Hold one of five items (shiny shell, wildflower, smooth rock, sparring stick, mossy snack), cycle with [ / ], press G near a dino to give it. A pure `giftReaction` scores the item's appeal against the dino's personality → loved / liked / neutral / disliked, and the affinity delta rides the existing friendship store + save. The reaction reads out in the dialog box. 7 new unit + 2 new e2e; 9/9 AC pass. Personality now changes what the player should do.

## Cycle 008 — 2026-05-30

- BACKLOG-048: In-character dino dialogue — APPROVED. Fixes the assistant-voice the live spot-check exposed: a hardened system prompt ("You are NOT an AI assistant…") with a one-shot in-character example, plus a pure `cleanReply` that strips wrapping quotes, drops assistant boilerplate, and keeps the first in-character sentence (falling back to a canned line if nothing survives). Deterministic guardrail means help-desk text can't reach the dialog box regardless of model output. 5 new/updated unit tests; voice-quality is a human re-greet follow-up.

## Cycle 007 — 2026-05-30

- BACKLOG-005: WebLLM-backed brain — APPROVED. `WebLLMBrain` runs Qwen2.5-0.5B in the browser via WebGPU, lazy-loaded on first greet behind the `NPCBrain` boundary (only `ai/webllmBrain.ts` imports `@mlc-ai/web-llm`, dynamic/code-split). Progressive + safe: instant canned fallback while loading or without WebGPU, never throws/blocks; one engine shared across all dinos. Prompt built from name/species/personality. 6 new/updated unit (incl. fake-engine generate path) + 2 new e2e; automatable AC pass. Live token generation deferred to a human spot-check (BACKLOG-047) — WebGPU confirmed available, but the verify harness couldn't drive a real greet.

## Cycle 006 — 2026-05-29

- BACKLOG-016: Friendship hearts — APPROVED. The first player-facing loop: greeting a dino raises affinity (0–100 points → 0–10 hearts), **C** toggles a collection panel listing all five dinos with heart bars, and the affinity persists in the IndexedDB save. Gain scaled by the dino's warmth/sociability (cycle-4 traits). Pure `friendship.ts`; additive save field (`SAVE_VERSION` unchanged, old saves default to empty). 7 new unit + 2 new e2e; 9/9 AC pass.

## Cycle 005 — 2026-05-29

- BACKLOG-017: Spawn 5 NPCs — APPROVED. A pure `ROSTER` (Rex, Mossback, Sunny, Twitch, Glade) with distinct species, spawn tiles, and colors; each dino's personality is seeded from its name (cycle 4). WorldScene spawns the cast via a loop reusing the `Dino` class; Rex anchored at `dinos[0]` for save/personality continuity. 4 new unit + 4 new e2e; 9/9 AC pass.

## Cycle 004 — 2026-05-29

- BACKLOG-010: NPC personality traits — APPROVED. 5 axes (curiosity, sociability, energy, agreeableness, bravery) seeded deterministically from a dino's name via `seededPersonality` (stable across reloads, not stored in save). `describePersonality` renders dominant poles into a brain-ready phrase. Traits flow through `NPCContext` (boundary intact) and the stub brain's mood now reflects them. 6 new unit + 2 new e2e; 9/9 AC pass.

## Cycle 003 — 2026-05-29

- BACKLOG-009: Save / load via IndexedDB — APPROVED. World survives a refresh: in-game time + player position restore on boot, auto-save fires each in-game hour, **E** exports a `dino-save.json`. Pure version-gated `serialize`/`deserialize` (`saveGame.ts`) split from raw-IndexedDB I/O (`saveStore.ts`); clock reused via new `set()`. 6 new unit + 5 new e2e; 9/9 AC pass. `version:1` seam left for BACKLOG-040 migration.

## Cycle 002 — 2026-05-29

- BACKLOG-008: Day/night palette shift — APPROVED. Full-map tint overlay lerps color + alpha across the day off the cycle-1 clock: midnight blue, warm dawn/dusk, clear noon. Pure `dayNight.ts` (`tintFor`, `dayPhase`); 6 new unit + 2 new e2e; 8/8 AC pass. Lights the runway for hour-keyed features (012 dawn plans, 014 dusk reflection, 041 night huddles).
- BACKLOG-046: Vite `host: true` — APPROVED. Fixed BUG-001 (IPv6-only bind); e2e now runs on the default Playwright config, retiring the cycle-1 `.qa-override` crutch.

## Cycle 001 — 2026-05-26

- BACKLOG-007: World tick clock — APPROVED. `WorldClock` class ships; 1 real second = 1 in-game minute; HUD `Day N — HH:MM` top-left; `onTick` / `onHour` listener API; 8/8 unit tests + 3/3 e2e green. Unblocks BACKLOG-008, -012, -041+.

## Cycle 000 — 2026-05-25 — Bootstrap

- Repo initialized
- CHARTER, BACKLOG, STYLE-GUIDE, CHANGELOG written
- Game scaffolded (Phaser 3 + TS + Vite, empty scene, placeholder dino)
- Studio scaffolded (7 routine prompts, state.json, chronicle, RE-ARM)
- CI workflow written
- Cron schedule created

## Cycle 101 — 2026-07-14 (Milestone 4 "The hunt has weight" — arcs 3 & 4)
- BACKLOG-442: the hunter's reputation — a herbivore chased twice by the same carnivore grows wary of that dino specifically, startling from it even off an active hunt (reuses the 367 flee branch; deathless, no save change).
- BACKLOG-438: a zone wants what it can't grow — the zone map lens reads each zone's demand (`wants <crop>◂<neighbour>`), leaning toward the neighbour out-growing the most of a crop it can't farm itself (a read; the food mover is queued as 444/446).

## Cycle 102 — 2026-07-15 (Milestone 4 "The hunt has weight" SHIPPED 🎉)
- BACKLOG-443: Predator/prey in the book — the food web is now legible. A carnivore reads its recent catches (`🦖 brought down N meal(s)`), a herbivore its recent escapes (`💨 slipped N hunt(s)`), a dino with no food-web history shows no line. Pure surfacing: new `world/foodweb.ts` `catchTally`/`escapeTally`/`foodwebStanding(diet, memories)` count the memory the hunt already files (437 `you brought down a meal` / 367 `you slipped X's hunt`) in the live 6-slot recall window; `BookRow.foodweb?` + a render line in `ui/lenses.ts`; `bookRows()` wires it via `foodwebStanding(dietOf(species,name), recall(...))`. Diet-keyed so catches/escapes never cross, hidden at 0 (like `knows N rumors`), reuses the 🦖/💨 hunt glyphs (no new art). No new state, no save change. +12 unit / 1 e2e. **Milestone 4 lore arc 3 ✅.** (lore track)
- BACKLOG-436: Need pulls the body — the need-drive finally moves the body. A pressing hunger/thirst leans a dino's wander toward relief (hunger → the hatch feeding zone; thirst → the grove pond, grove-only, the one place thirst is slaked — elsewhere it just wanders, deferring the local waterhole to 445). A genuine *lean*: `world/needs.ts` `needSeeks(roll, chance=NEED_PULL_CHANCE=0.6)` gates it, and the `seeking` branch sits **below every ritual** in `forceStep` (huddling/gathering/moping/ticcing/socializing all still win; a pressing need already makes a dino "disturbed" so the tic yields; keeper drops still win the food rush). `world/zones.ts` `grovePondTile(cols)`; `WorldScene.needTargetFor(d, need)` + `__needTarget`/`__needStep` hooks. Deathless + additive (rides the 371 `needs` map, no new save field). +5 unit / 3 e2e. **Milestone 4 structure arc 2 ✅ — milestone complete.** (structure track)

## Cycle 104 — 2026-07-17 (Milestone 5 "No one goes hungry" — arcs 2 & 2)
- BACKLOG-376: Woke hungry — hunger stops being a gauge and becomes a moment. Since cycle 80 a dino's hunger has been a float crossing 0.6 with nothing to catch; dawn is the one boundary the whole cast observes together (the chorus, 192), and until now that morning was uniform. New pure `world/wake.ts`: `wokeHungry(n)` (reads `.hunger` against `NEED_THRESHOLD` directly — **not** `pressingNeed`, which answers "the *more* pressing need" and would rob a hungry-and-thirstier dino of its beat, pinned by a unit test), `wakeHungryLine(name, traits)` (shaded on two axes — a prickly dino wakes irritable, a high-energy one impatient, everyone else plaintive; deterministic procedural, no LLM, total without traits), `wakeHungryMemory()`. `checkWakeHungry()` runs synchronously at the **tail of `checkDawnChorus`**, inheriting its once-per-day + live-only guards instead of duplicating a clock. Who wakes hungry is a read on temperament for free: `needs.ts` scales the hunger rate by energy (0.6×–1.4×). A read, not an actuator (436 pulls the body; 444 feeds it). Transient `lastWokeHungry`, no save change. +9 unit, +4 e2e. **Milestone 5 lore arc 2 ✅.** (lore track)
- BACKLOG-444: A carrier feeds the hungry — the pantry gets a door. 446 banked food into a store nothing could spend; this is the first time the economy half (gather→bank→carry→build) and the need half (hunger/thirst/the hunt) of this park touch each other. `needs.ts` gains `STARVING = 0.9` + `isStarving()`; `foodstore.ts` gains `takeFood`/`pickFoodToSpend` (twins of `resource.ts`'s `takeResource`/`pickCarry`: the dino's favorite if the zone banked it, else the most-stocked id, FOODS-order tiebreak) + `storesFedLine`/`storesFedMemory`. `feedFromStores()` on the `checkNeeds` tick spends one unit from the dino's **home** zone (not the viewed one) when it's starving, no keeper drop is in play, and the zone is stocked → hunger 0, food-emoji flash, `<emoji> <Zone>'s stores fed <name>` ticker, a memory. The store is the last resort, not the default: **the 0.6–0.9 band is load-bearing** (it's where 376's dawn beat, 436's need-pull and the 🍖 tell live) and is double-guarded by an e2e (a dino at 0.7 with a full pantry stays hungry) plus a unit test pinning `STARVING > NEED_THRESHOLD`. Deathless — a spend bar, not a death clock; mortality stays the operator's CHARTER call. "Carrier" read as the 429 carry/flow *system*, not a courier animation (447 owns ferrying between zones). No save change (`foodPileByZone` was already additive). +14 unit, +5 e2e. **Milestone 5 structure arc 2 ✅.** (structure track)

